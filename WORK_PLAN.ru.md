# План работ

> Источник — аудит 2026-09-26 (`docs/audit/2026-09-26_full_audit/REPORT.ru.md`). Здесь только
> **подтверждённые** и выполнимые пункты. Английская версия — `WORK_PLAN.md`.
> Исполнитель по правилу №16: **Д** — DeepSeek (механика по карточке), **К** — Claude (ведущий),
> **В** — владелец (sudo на устройстве, решения). Сложность: S / M / L.

## Волна 1 — живая система, сейчас

| # | Пункт | Кто | Слож. | Тесты / проверка |
|---|---|---|---|---|
| 1.1 | **API-1:** вынести `os.statvfs`/`exists` по HDD в `asyncio.to_thread` + таймаут в `downloads._disk_free`, `storage._disk_info`, `system._read_disk`; при таймауте — `disk_down` | К | S | тест: зависающий `disk_free` не держит цикл дольше таймаута; `/healthcheck` отвечает |
| 1.2 | **OPS-2:** ежедневный отчёт и offsite-скрипт берут `VPS_HOST` из `/opt/nasa/config/.env`, а не зашитый адрес; переустановить `/usr/local/sbin/nasa-send-report-telegram.sh` | К + В (sudo) | S | прогон `--test`: сообщение пришло |
| 1.3 | **OPS-1:** сторож контейнеров на хосте — systemd-таймер раз в 2 мин поднимает ожидаемые `homecloud_*`, если они `exited` не по плану, и перезапускает `unhealthy`; алерт в Telegram; не трогает контейнеры из списка ручного обслуживания | К + В (sudo) | M | живой тест: `docker kill` → контейнер `Up` ≤ 3 мин |
| 1.4 | **CF-2:** ротация логов Docker — `log-opts` `max-size=10m`, `max-file=3` в `daemon.json` (+ дефолт в compose) | К + В (sudo, рестарт Docker в окно) | S | `docker inspect` показывает `max-size` |
| 1.5 | **HK-1:** атомарный перенос в `on_complete.sh` (`mv -n` + повтор со следующим суффиксом или `flock` на каталог) | Д (тест) + К | S | параллельный тест двух хуков с одним именем: оба файла целы |

## Волна 2 — безопасность

| # | Пункт | Кто | Слож. | Тесты / проверка |
|---|---|---|---|---|
| 2.1 | **GW-3:** русские триггеры секретов в `TOKEN_RE` (пароль, ключ, код, пин, секрет), разделитель — `:`/`=`/пробел | К | S | набор русских фраз с паролем/кодом редактируется |
| 2.2 | **API-2/3:** сетевой барьер — контейнеру aria2 и исходящим запросам API запретить `192.168.0.0/16`, `10/8`, `172.16/12`, `127/8`, `169.254/16` (iptables `DOCKER-USER` на Jetson); в коде — `follow_redirects=False` + проверка `Location` | К + В | M | мок редиректа на внутренний адрес → отказ; с устройства `curl` из контейнера на `192.168.0.1` не проходит |
| 2.3 | **SH-1:** `cloudru_iam_token_example.sh` — ответ через stdin, не через heredoc | Д | S | фейковый ответ с `'''` не исполняет код |
| 2.4 | **CI-1 / SEC-1:** trivy на тег+SHA; gitleaks без `--no-git` (история) | К | S | job падает на тестовом секрете в истории ветки |
| 2.5 | **DP-2:** NAS API не от root; `docker.sock` через socket-proxy только с `GET /containers/json` | К | M | `test_api_access` + ручной `/v1/containers` |
| 2.6 | **DP-1 / DEP-2:** закрепить версии образов (netdata, portainer, samba, beszel, nextcloud, immich); проверка «нет `:latest`» в воротах | Д (инвентарь) + К | S | gate-grep |
| 2.7 | **DEP-1:** закрепить зависимости NAS API (`==` + lock с хешами); оценить замену `python-jose`/`passlib` | К | S/M | `service-tests` зелёные после пина |
| 2.8 | **SH-2 / HK-2:** общий `tg_send()` в `scripts/lib/` — токен не в argv; временный файл на VPS через `mktemp` | Д + К | M | grep-проверка: нет `bot${TELEGRAM_BOT_TOKEN}` в argv |
| 2.9 | **SEC-2:** `check_no_secrets.sh` сканирует `*.md` с узким allowlist | Д | S | тестовый секрет в `.md` ловится |

## Волна 3 — надёжность и качество

| # | Пункт | Кто | Слож. | Тесты / проверка |
|---|---|---|---|---|
| 3.1 | **SD-2:** убрать литеральные пути из 14 юнитов → `${NAS_JETSON_NANO_PROJECT_DIR}` / `/etc/nas-layout.env`; guard в воротах | Д + К | M | grep-guard: нет `/home/admin`, `/opt/nasa` в `systemd/*.service` |
| 3.2 | **SD-1:** `nas-offsite-backup.sh` — проверка непустого `$WORKDIR` перед `restic backup` | Д | S | тест «пустой поток → exit 1» |
| 3.3 | **GW-1 / GW-2:** единый нормализованный ключ пользователя; резервирование бюджета под локом до вызова | К | M | регистр не обходит лимит; N параллельных запросов не превышают лимит больше чем на один «в полёте» |
| 3.4 | **SD-3:** `flock` на `/mnt/hdd2tb` во всех скриптах обхода HDD | Д + К | S | два запуска: второй сразу получает отказ |
| 3.5 | **QA-1/2/3:** vermin в CI; переименовать пакеты `app` или изолировать прогон; `tests/unit` собираться через pytest | Д | S/M | CI зелёный, общий прогон без ложных падений |
| 3.6 | **API-4/5:** закрывать клиент Telegram; пределы на списки состояния и outbox | Д | S | тест закрытия, тест вытеснения |
| 3.7 | ротация `jms583-health.log`, удаление `nasa-api.jsonl.*` после миграции | К + В | S | — |
| 3.8 | **BK-1:** если backup-api будет включаться — санация имени файла + `USER`; иначе убрать «боевой» код до RFC | К | S | тесты traversal |

## Решения владельца

- 2.2: сетевой барьер меняет iptables на Jetson — нужно окно и согласие.
- 1.4: рестарт Docker — окно обслуживания (все сервисы на ~1 мин).
- 3.8: backup-api — чинить или убрать.
