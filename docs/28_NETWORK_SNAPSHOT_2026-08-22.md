# 28. Слепок сети и восстановление работоспособности — 2026-08-22

> 🇷🇺 Домашняя сеть перестроена (подтверждено владельцем). Этот документ — **слепок
> фактического состояния**, снятый живыми командами 2026-08-22, и разбор трёх дефектов,
> которые в этот день были найдены и устранены. Всё, что здесь помечено ✅, измерено,
> а не перенесено из прошлой документации.
>
> 🇬🇧 The home network was rebuilt. This is a **measured snapshot** taken on 2026-08-22,
> plus the three defects found and fixed that day.

Предыдущая точка: [`plans/CHECKPOINT_2026-08-11.md`](plans/CHECKPOINT_2026-08-11.md).
Регламент, по которому перестройка планировалась: [`27_HOME_NETWORK_MESH.md`](27_HOME_NETWORK_MESH.md).

---

## 1. Обязательное условие всех работ

🔴 **VPN на VPS сохраняет работоспособность; конечные пользователи не затрагиваются.**
Задано владельцем 2026-08-22 и действует для любой последующей работы, а не только
для этой. Проверяется **до и после** каждого изменения:

```bash
docker ps --format '{{.Names}}\t{{.Status}}'          # amnezia-* не перезапускались
ss -tlnp | grep -E '0.0.0.0:443|:22 '; ss -ulnp | grep 40568
docker exec amnezia-awg2 wg show | grep -c '^peer:'   # число пиров не уменьшилось
```

Замер после всех сегодняшних изменений: `amnezia-xray` up 2 недели, `amnezia-awg2`
up 6 недель — **13 пиров, 10 с активным рукопожатием**; наружу слушают только
22, 443/tcp и 40568/udp. Ни один контейнер Amnezia не перезапускался. ✅

Правка nginx выполнялась **`nginx -s reload`**, а не пересозданием контейнера:
`nasa_nginx` сохранил аптайм 6 недель, разрыва сессий не было.

---

## 2. Сеть как она есть (замерено)

```
                    Интернет (провайдер)
                              │
                    ┌─────────────────────┐
                    │  EC220-G5           │  192.168.0.1
                    │  5C:62:8B:4D:82:8C  │  остался шлюзом
                    └──────────┬──────────┘
                     ├─────────┴──────────┐
                     ▼                    ▼
        Jetson Nano 192.168.0.50    Deco  WAN 192.168.0.107
        eth0 1000 Мбит/с            B8:FB:B3:B8:64:6D
        (в гигабите, не тронут)            │
                                    ┌──────┴──────────────┐
                                    │  Deco  LAN          │  192.168.68.1/22
                                    │  B8:FB:B3:B8:64:6C  │  DHCP, Wi-Fi
                                    └──────┬──────────────┘
                                           ▼
                      рабочая станция, телефоны — 192.168.68.0/22
```

**Ключевое:** Deco введён **вторым роутером позади EC220-G5**, в своей заводской
подсети `192.168.68.0/**22**` (маска `255.255.252.0` — заводская у Deco; это /22,
а не привычная /24: адреса `192.168.68.0`–`192.168.71.255`). Это двойной NAT. Шаги 2, 3 и 6 регламента
([`27_HOME_NETWORK_MESH.md`](27_HOME_NETWORK_MESH.md) — перевод LAN IP на `192.168.0.1`,
перенос Jetson в Deco, вывод EC220) **не выполнялись**.

| Что | Замер 2026-08-22 | Как проверено |
|---|---|---|
| Рабочая станция | `192.168.68.105/22`, шлюз `192.168.68.1` | `Get-NetIPAddress`, `Get-NetRoute` |
| Телефон владельца | `192.168.68.100/22` (Redmi Note 15 Pro, Android 15) | `adb shell ip addr` |
| Живые узлы в `192.168.0.0/24` | ровно два отвечают на ping: `.1` и `.50`; в ARP есть ещё `.107` | ping-свип 254 адресов с Jetson |
| Jetson | `192.168.0.50/24`, шлюз `192.168.0.1`, **Speed 1000** | `ip addr`, `/sys/class/net/eth0/speed` |
| Маршрут станция → Jetson | `192.168.0.0/16` → next hop `192.168.68.1` | `Find-NetRoute` |
| Сервисы NAS из Wi-Fi-подсети | 8080, 2283, 8090, 8099, 445, 3001, 19999 — **все открыты** | `Test-NetConnection` |
| Перезагрузка Jetson | 2026-08-17 11:05 UTC | `last -x reboot` |

Опознание Deco — по OUI TP-Link (`B8:FB:B3`) и заводской подсети `192.168.68.x`;
модель по сети не подтверждается, приложением не проверялась.

### Что из этого следует — честно

**Хорошее, и его больше, чем ожидалось:**

- Правило №3 не нарушено: Jetson не трогали, адрес `.50` и подсеть `192.168.0.0/24` целы.
- **Jetson остался в гигабите.** Цена перестройки из регламента (1000 → 100 Мбит/с)
  ещё не заплачена — он висит на порту EC220, а не Deco.
- EC220-G5 не выведен, то есть путь отката (правило №11) сохранён физически.

**Плата:**

- Телефоны и станция живут за **вторым NAT**. До NAS они достают по IP, и это работает,
  но обзор сети и обращение по имени между подсетями — нет.
- Jetson **не может сам инициировать** соединение к устройствам за Deco.
- Гостевой изоляции из Фазы 7 нет: сегментации внутри LAN как не было, так и не появилось.
  Замечание tklim в этой части по-прежнему открыто.

---

## 3. Три дефекта, найденные и устранённые 2026-08-22

Повод: в Telegram-отчёт приходили три ❌ по внешнему доступу, и не работала
синхронизация на телефонах.

### 3.1. 🔴 Nextcloud навязывал всем клиентам мёртвый адрес

**Симптом.** Телефоны не синхронизируются.

**Замер.** Запрос с любого адреса — из дома и снаружи — получал один ответ:

```
GET http://192.168.0.50:8080/     →  302  Location: https://193.8.215.130:8443/login
POST /index.php/login/v2          →  {"login":"https://193.8.215.130:8443/login/v2/flow/…"}
```

То есть телефон, стоящий в двух метрах от NAS, отправлялся на адрес VPS.

**Причина.** В конфигурации остались значения от старого адреса VPS:

```
overwritehost      = 193.8.215.130:8443
overwriteprotocol  = https
overwrite.cli.url  = https://193.8.215.130:8443
```

Адрес VPS сменился 2026-08-09 (`193.8.215.130` → `95.163.176.103`), но эти три ключа
не поправили. Важная поправка владельца, подтверждённая замером: **старый адрес не
«мёртв» — VPS держит оба**, `193.8.215.130/32` и `95.163.176.103/32` на `enp0s3`.
Он недостижим по пути: заблокирован российскими ISP, а сервисные порты и без того
закрыты для не-VPN источников. Для клиента под VPN рабочий адрес — внутренний
`172.29.172.1`, потому что публичные IP исключены из туннеля по определению
(иначе VPN зациклится). Замер с рабочей станции под VPN:

```
https://193.8.215.130:8443/status.php → соединение закрыто
https://95.163.176.103:8443/status.php → таймаут
http://172.29.172.1:8080/status.php    → HTTP 200
```

**Исправление — не «поменять адрес на новый», а перестать навязывать адрес вообще:**

```bash
occ config:system:delete overwritehost
occ config:system:delete overwriteprotocol
occ config:system:set overwrite.cli.url --value=http://172.29.172.1:8080
occ config:system:set trusted_domains 4 --value=95.163.176.103
occ config:system:set trusted_domains 5 --value=borovskoy.dynv6.net
occ config:system:set trusted_domains 6 --value=172.29.172.1
```

Теперь каждый клиент остаётся на том адресе, по которому пришёл. Схема выдерживает
следующую смену IP: чинить будет нечего.

**Проверено после:**

```
дома:      GET /  →  302 Location: http://192.168.0.50:8080/login          ✅
под VPN:   login/v2 → "endpoint":"http://172.29.172.1:8080/login/v2/poll"  ✅
чужой Host: evil.example.com → страница «недоверенный домен»               ✅ защита вернулась
```

**Побочный эффект, который стоит назвать:** пока стоял `overwritehost`, проверка
доверенного домена была фактически выключена. Сняв навязывание, мы её включили обратно —
это плюс к безопасности, но именно поэтому пришлось перечислить все реальные адреса.

**Откат:** три `occ config:system:set` с прежними значениями (выписаны выше).

### 3.2. 🟠 nginx на VPS терял порт в адресе

**Замер.** Сразу после 3.1 VPN-клиент получал `Location: http://172.29.172.1/login` —
**без `:8080`**, то есть на порт 80, где ничего не слушает и куда ufw не пускает.

**Причина.** В HTTP-блоке `nextcloud.conf` стояло `proxy_set_header Host $host` —
`$host` отбрасывает порт. В HTTPS-блоке был правильный `$http_host`.

**Исправление** на VPS, `/opt/nasa/nginx/conf.d/nextcloud.conf`: `$host` → `$http_host`,
затем `nginx -t` и `nginx -s reload` (без пересоздания контейнера).

**Проверено:** `"endpoint":"http://172.29.172.1:8080/login/v2/poll"` — порт на месте. ✅

**Откат:** `/opt/nasa/nginx/conf.d/nextcloud.conf.bak.20260822`.

### 3.3. 🔴 Правка 3.1 сломала Talk-бота. И я объяснил это историей вместо замера

Этот раздел переписан в тот же день. Первая редакция была неверной, и ошибка
поучительнее самой поломки — поэтому она остаётся здесь целиком, а не удаляется.

**Что произошло на самом деле.** Сняв `overwritehost` (раздел 3.1), я вернул к работе
проверку доверенного домена — она была фактически выключена, пока стояло навязывание
адреса. Talk-бот опрашивает Nextcloud по docker-имени **`host.docker.internal`**,
которого в `trusted_domains` не было. Nextcloud стал отвечать ему `400` с HTML-страницей
на 22 КБ вместо OCS-JSON:

```
{"error": "Trusted domain error.", "code": 15}
```

Штатный long-poll блокируется на 30 секунд; отказ возвращается мгновенно. Шесть комнат
превратились в плотный цикл повторов: **213 % CPU** у Nextcloud, `load average` 5.89.

**Исправление:**

```bash
occ config:system:set trusted_domains 7 --value=host.docker.internal
```

После — снова `304 Not Modified`, CPU 9.9 %, нагрузка вернулась к обычной.
Проверено вживую: отправленное в комнату `нас пинг` получило ответ
`🏓 pong · uptime 4d 22h 18m` за 25 секунд.

**А вот моя ошибка.** Увидев 400-е, я объявил их застарелым дефектом: «бот двенадцать
дней бил в стену с выкатки Фазы C, 16 300 отказов в сутки». Записал это в документацию
и в два коммита. Базовая линия говорит обратное:

```
вчера, за сутки:   13 626 опросов  →  ВСЕ 304 Not Modified, ноль 400
сегодня 00–10 ч:   по 678 в час    →  все 304
сегодня час 11:    7 975 × 400     ←  ровно окно моих правок
```

678 опросов в час — это и есть норма: шесть комнат × long-poll по 30 секунд ≈ 12 в
минуту. Я принял штатный ритм за шторм, потому что смотрел на суточное число, не сверив
его с ожидаемым, и не посмотрел тот же показатель **до** своей правки.

То есть я нашёл, диагностировал и починил **собственную регрессию двадцатиминутной
давности**, приписав её прошлому. Проверка стоила одной команды.

**Что при этом осталось правдой.** Правка `trusted_domains` нужна и остаётся: без неё
конфигурация из 3.1 не работает. Но `213 % CPU` и `load 5.89` — это не «самодельная
нагрузка, которая копилась неделями», а мои же двадцать минут. Никакой части замечания
tklim про «4 ГБ мало» это не закрывает.

**Урок** — тот же, что уже записан про Vostro: объяснение появилось раньше, чем была
проверена входная величина. Правило: увидел аномалию — сначала посмотри тот же
показатель **до** собственной правки.

### 3.4. 🟠 Отчёт в Telegram кричал волком

**Симптом.** Три ❌ по внешнему доступу и «Beszel Hub unreachable».

**Причина двойная:**

1. В `/etc/nasa-monitor/nasa-monitor.env` от 23 июня осталось `SERVER_IP="193.8.215.130"`.
   По этому же адресу ходил и Beszel-запрос — отсюда его «unreachable».
2. **Проверка была бессмысленна по построению.** Она curl-ила сервисные порты VPS
   с публичного выхода Jetson — а Фаза 1 закрыла их для всех, кроме VPN. То есть
   ❌ был бы и с правильным адресом, всегда.

**Исправление.** Адрес поправлен, а сама проверка переписана:

- цепочка `клиент → nginx → обратный туннель → Jetson` меряется **на самом VPS**
  против `127.0.0.1` — это тот же путь, которым идёт запрос VPN-клиента;
- взяты настоящие health-эндпоинты: `/status.php`, `/api/server/ping`, `/health`,
  `/healthcheck` (прежняя проверка дёргала `/`, который у шлюза и API отдаёт 404);
- наружная проверка **инвертирована**: теперь тревога — это порт, который **ответил**
  из интернета. Закрытый порт — ✅, как и должно быть по правилу №4.

**Проверено:** отчёт печатает пять ✅ через туннель, `ports closed to the internet —
rule #4 holds`, и Beszel снова видит оба агента.

**Откат:** `/usr/local/sbin/nasa-daily-report.sh.bak.20260822`,
`/etc/nasa-monitor/nasa-monitor.env.bak.20260822`.

---

## 4. Проверка на самом телефоне (adb, 2026-08-22)

Владелец подключил телефон к рабочей станции; проверено на устройстве, а не со слов.
Redmi Note 15 Pro, Android 15, `192.168.68.100/22`.

### 4.1. Главное: у приложений **нет** пути по домашней сети

```
ping 192.168.68.1 (свой же шлюз)  →  100 % потерь
nc 192.168.0.50 8080              →  timeout
ping 172.29.172.1 (NAS через VPN) →  57 мс, TCP 8080 и 2283 открыты
ip route get 192.168.0.50         →  dev tun1  src 10.8.1.16
```

VPN на телефоне включён как **always-on** (`always_on_vpn_app = org.amnezia.vpn`)
и забирает весь трафик, включая локальный. Разбор правил маршрутизации показал,
что исключены из туннеля ровно **шесть** приложений (uid 10148, 10152, 10190,
10325, 10334, 10341). Наших среди них нет:

| Приложение | uid | Путь |
|---|---|---|
| Nextcloud | 10346 | через VPN |
| Immich | 10345 | через VPN |
| DAVx⁵ | 10403 | через VPN |
| Nextcloud Talk | 10405 | через VPN |

**Следствие, которое стоит осознать:** каждая фотография, снятая дома, уходит
`телефон → AmneziaWG → VPS во Франкфурте → обратный SSH-туннель → Jetson`
и возвращается в тот же дом. Гигабитный линк Jetson при этом не используется вовсе.

Это не поломка — это работает. Но это объясняет, почему синхронизация «ощущается
мёртвой», и это счёт за трафик VPS.

### 4.2. Что на телефоне на самом деле работает

Проверено вживую, обе стороны — на телефоне и в журнале сервера:

| Что | Результат |
|---|---|
| Nextcloud (файлы) | ✅ `PROPFIND /remote.php/dav/files/admin/` → **207**, сервер видит запрос с `172.29.172.4` |
| DAVx⁵ (контакты) | ✅ `Sync worker succeeded`, `SyncResult(... numAuthExceptions=0, numHttpExceptions=0, numIoExceptions=0)` |
| Immich | ✅ websocket подключён; **все снимки камеры выгружены** — последний файл на телефоне 21.08 13:17, последний ассет на сервере 21.08 19:30 МСК |
| Talk | приложение установлено, отдельно не проверялось |

До правок сегодняшнего дня история синхронизации показывала отказы
(`PERIODIC FAILURE` 05:47, `LOCAL FAILURE` 07:44 и 07:49). После — успех.
Связь по времени есть; утверждать причинность **не буду** — в тот же промежуток
менялась конфигурация Nextcloud, и раздельно это не замерялось.

### 4.3. Единственная настоящая ошибка на телефоне

```
[Api:AssetMediaService] Unsupported file type вектор.cdr
```

**Ровно раз в час, каждый час.** Это файл CorelDRAW, попавший в альбом, выбранный
для резервного копирования. Immich отвергает его по определению — это не фото и
не видео. Очередь при этом **не заблокирована**: новых снимков просто нет.

Лечение — на телефоне: убрать этот альбом (или файл) из списка для автозагрузки.
Серверу править нечего.

### 4.4. Скрытый риск, найденный попутно

Аккаунт Nextcloud на телефоне прописан как `admin@193.8.215.130:8443`, а сертификат
на VPS выписан **только** на этот адрес:

```
X509v3 Subject Alternative Name: IP Address:193.8.215.130, DNS:localhost
notAfter=Jun 22 06:37:45 2036 GMT
```

То есть телефон держится на старом адресе VPS, и держится он **только пока жив
туннель**: снаружи этот IP заблокирован российскими провайдерами. Если адрес
однажды снимут с `enp0s3`, телефон встанет — и переезд на `95.163.176.103` или
`borovskoy.dynv6.net` **потребует перевыпуска сертификата**, потому что в текущем
их нет. Это не срочно, но это долг, о котором лучше знать заранее, чем узнать в
момент отказа.

---

## 5. Адресный план после адаптации

Единого «внешнего адреса» у системы нет и не должно быть — у каждого пути свой:

| Откуда | Адрес | Условие |
|---|---|---|
| Дома (Wi-Fi за Deco, `192.168.68.x`) | `http://192.168.0.50:<порт>` | работает через NAT Deco |
| Дома (проводом в EC220) | `http://192.168.0.50:<порт>` | — |
| Снаружи под VPN | `http://172.29.172.1:<порт>` | **рабочий внешний путь** |
| Снаружи без VPN | нет | и не должно быть — правило №4 |
| По имени | `borovskoy.dynv6.net` | A → `95.163.176.103`; сервисные порты всё равно только из VPN |

Публичные IP VPS (`95.163.176.103`, `193.8.215.130`) в клиентах **не прописывать**:
из-под VPN они недостижимы конструктивно, а без VPN закрыты firewall'ом.

---

## 6. Приёмка

- [x] VPN на VPS не затронут: 13 пиров, 10 рукопожатий, контейнеры Amnezia не перезапускались
- [x] Наружу открыты только 22, 443/tcp, 40568/udp
- [x] Дома Nextcloud держит клиента на `192.168.0.50` (302 → LAN)
- [x] Под VPN вход отдаёт `http://172.29.172.1:8080/...` с портом
- [x] Talk-бот снова получает `304` и **отвечает вживую**: `нас пинг` → `🏓 pong · uptime 4d 22h 18m` за 25 с (регрессия из 3.3 закрыта)
- [x] Отчёт меряет цепочку через туннель и тревожится на открытый порт
- [x] 13 контейнеров up, бэкап свежий (22.08 03:14), туннель active
- [x] **Проверка с телефона владельца выполнена** — Nextcloud 207, DAVx⁵ SUCCESS, Immich выгрузил всё
- [ ] Убрать `вектор.cdr` из альбомов автозагрузки Immich (действие на телефоне)
- [ ] Решить, оставлять ли фотографии ходить через Франкфурт (см. 4.1)

---

## 7. Что осталось открытым

| # | Что | Примечание |
|---|---|---|
| 1 | Шаги 2/3/6 регламента Deco | LAN IP → `192.168.0.1`, перенос Jetson, вывод EC220. Пока двойной NAT |
| 2 | Гостевая сеть и сегментация LAN | Фаза 7; замечание tklim в этой части не закрыто |
| 3 | Волна 0 — off-site бэкап | Ключ на Vostro создан, нигде не авторизован |
| 4 | Расхождение git ↔ устройство | Отчётный скрипт синхронизирован сегодня; остальное — нет |

---

## 8. Грабли, добавленные этим днём

1. **Отчёт, который каждый день показывает ❌ на штатное состояние, — сломанный отчёт.**
   Три креста по внешнему доступу были нормой с Фазы 1 и приучили их не читать.
   Проверять надо то, от чего зависит человек, а тревожиться — на нарушение правила,
   а не на его соблюдение.
2. **Смена IP ломает не один файл, а список файлов.** После 09.08 старый адрес остался
   в `overwritehost`, `overwrite.cli.url`, `trusted_proxies`, `nasa-monitor.env` и в
   десятке install-скриптов. Правильный вывод не «поправить адрес», а **перестать
   хранить адрес там, где можно не хранить**.
3. **`$host` в nginx отбрасывает порт, `$http_host` — нет.** На нестандартном порту
   это ломает любой сгенерированный редирект.
4. **Nextcloud отвечает на недоверенный домен кодом 400 и HTML-страницей.** Клиент,
   ожидающий JSON, видит «Bad Request» и не сообщает настоящей причины — она в теле.
5. 🔴 **Симптом, замеренный внутри собственного окна правок, нельзя объяснять историей.**
   Главные грабли дня, и наступил на них я. Увидев 400-е и `load average` 5.9, я
   объявил это застарелым дефектом и записал в документацию и в два коммита.
   Базовая линия — одна команда — говорила обратное: вчера **13 626 опросов, все `304`,
   ноль `400`**. Правило: увидел аномалию — сначала посмотри тот же показатель **до**
   своей правки. Родня уже записанным граблям про Vostro.
6. **Снятие `overwritehost` включает обратно проверку доверенного домена.** Пока оно
   стояло, проверка была фактически выключена. Меняя его, сразу перечисляй **все** имена,
   которыми к Nextcloud обращаются изнутри, — иначе внутренние клиенты (у нас Talk-бот
   по имени `host.docker.internal`) начнут получать `400` на каждый запрос.
