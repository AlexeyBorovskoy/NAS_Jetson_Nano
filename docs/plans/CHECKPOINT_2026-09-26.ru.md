# Точка проекта 2026-09-26 — сеть восстановлена, аудит, голос, самовосстановление

> Репозиторий: `main` @ `f4dc67a` (37 коммитов с точки 22.09), GitHub запушен.
> Устройство: `~/nasa` @ `2436b63`, 15 контейнеров, аптайм 1 нед 21 ч (перезагрузка ~18.09).
> Предыдущая точка — `CHECKPOINT_2026-09-22.md`. Английская версия — `CHECKPOINT_2026-09-26.md`.

## 1. Инциденты дня и отозванное

| Что | Причина | Как нашли | Исправление |
|---|---|---|---|
| Обратный туннель Jetson→VPS лежал **18–26.09** (доступ снаружи, SOCKS до Telegram, ежедневный отчёт) | домашний провайдер заблокировал **новый** IP VPS `95.163.176.103`, старый `193.8.215.130` снова проходит (зеркально августу); ключ хоста совпал — тот же VPS | `bash </dev/tcp` с Jetson: 22/443 FAIL, старый IP ok; станция видит VPS через VPN, идущий на старый IP | `VPS_HOST=193.8.215.130` в `/opt/nasa/config/.env` (выполнил владелец: классификатор прав блокирует sudo-правки агента) |
| Telegram-бот **молчал 6 суток** при «здоровом» контейнере | `socksio.ProtocolError` не наследует `httpx.HTTPError` — цикл опроса умер 20.09 молча | одна строка `unreachable` за 6 дней при лежащем SOCKS | `supervise()` + `/healthcheck.telegram` (`db458f7`) |
| Торренты Ивана не начинались | на паузе aria2 отдаёт `totalLength=0`, код ждал размер вечно | `tellWaiting`: размер 0, файлы с длинами | `total_size()` по файлам |
| После пересоздания aria2 закачки «потерялись» | в сессии хранится magnet, GID торрента меняется | журнал `error`, в aria2 новые GID | `_reconcile()` по info-hash/имени (`7c63e67`), проверено `docker kill` |
| Контейнер после `docker kill` **не поднимается** | `restart: always` не действует на ручную остановку | живой тест: 134 с `Exited (137)` | сторож контейнеров (OPS-1) — в git, ставит владелец |
| Классификатор отвечал сводкой о сервере на общий вопрос | подстрочные шаблоны («температур», «помощ», «альбом»…) | голосовой вопрос владельца, журнал `bobik home tool` | сильные/слабые шаблоны + домашний якорь + анти-якорь (`1a221f8`), 38 регрессионных фраз |

**Отозвано:** мой «тест 1 — aria2 упал и поднялся за 9 с» — aria2 это PID 1, `kill -9` изнутри его не берёт, падения не было; повторено `docker kill` → нашлось OPS-1. Две находки субагентов аудита опровергнуты при сверке (SD-4, CF-1).

## 2. Сделано и в бою (проверено по делу, не по файлам)

- **Закачки:** всегда HDD, папка на человека `Downloads/<логин>`; атомарный перенос без затирания (HK-1, подтверждён на двух одинаковых MathCAD); **5 закачек Ивана завершены** (SolidWorks 16 ГБ, KOMPAS 12 ГБ, MATLAB 13 ГБ, MathCAD ×2), сообщение в группу отправлено; уведомления о закачках дублируются в группу с «👤 логин:».
- **API-1:** блокирующий I/O по дискам — в поток с таймаутом (`app/blocking.py`), зависший ntfs-3g больше не останавливает API/бота/закачки.
- **Голосовые (E10):** `homecloud_stt` (Vosk small-ru, 195/320 МБ); прогон Golos: «афина я не хочу чтобы сегодня звонил будильник» — **слово в слово**, 3.1 с на 3.7 с звука; первое голосовое владельца обработано (`voice_ok: 1`). Текст не журналируется.
- **Immich:** версия закреплена `v2.7.5` (git + устройство, тег на тот же образ), ворота не пропустят плавающий тег.
- **backup-api:** две path traversal закрыты, non-root (сервис не развёрнут).

## 3. В git, ждёт владельца

| Что | Действие владельца |
|---|---|
| Волна 1: отчёт (адрес VPS из `.env`), сторож контейнеров, сетевой барьер aria2, ротация логов Docker + перезапуск Docker | `cmd /c "ssh admin@192.168.0.50 bash -s < …\scratchpad\install_wave1.sh"` |
| Immich ML на станции: compose, `immich_ml_station.ps1`, `set_ml_url.sh`, runbook (`IMMICH_ML_STATION_RUNBOOK.ru.md`) | включить гипервизор (dism + bcdedit + перезагрузка), Docker Desktop. Ключ API уже есть в `.env` (права на system-config подтверждены 200) |
| Голос: проверка приватности в группе (голосовое без «бобика» — молчание) | отправить голосовое в группу |
| Объявление семье о голосе и уведомлениях (правило №18) | после проверки выше |

## 4. Аудит 2026-09-26

`docs/audit/2026-09-26_full_audit/REPORT(.ru).md`, план — `WORK_PLAN(.ru).md`. Закрыто сегодня: API-1, HK-1, BK-1/2, OPS-2 (в git), OPS-1/CF-2/API-2-3 (в git, ставит владелец), D6 Immich. Открыто главное: GW-3 (русские секреты мимо фильтра шлюза), SSRF-код в `_head_size`, SD-2 (пути в юнитах), CI-1/SEC-1, DEP-1, DP-1/2.

## 5. Новые правила

- №15 — GitHub целиком EN, GitVerse RU, пары `X.md`/`X.ru.md` + скрипт публикации (**скрипт не написан, 258 документов не разнесены**).
- №16 (+дополнение) — ведущий не пишет код реализации: Sonnet/Haiku-субагенты и DeepSeek (`tools/deepseek-worker`, ADR-0012).
- №17 — из дома прямой ssh на Jetson.
- №18 — объявлять семье о новых возможностях после проверки в бою.

## 6. Известные хвосты

- В `stop` скрипта станции нет возврата `machineLearning.urls` — добавить до первого запуска (баг Immich #27617: недоступный ML-URL → OOM `immich-server`).
- DeepSeek регулярно исчерпывает ходы до коммита (4 раза из 9) — ведущий дописывает; поднять `max_turns` или дробить карточки.
- VPN на телефоне медленный — не мощность VPS (610 Мбит/с наружу, CPU простаивает); рекомендация — раздельное туннелирование в приложении.
- `tests/unit` не собирается pytest'ом (QA-3), три тестовых каталога одним процессом — 43 ложных падения (QA-2).
