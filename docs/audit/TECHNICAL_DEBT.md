# Реестр технического долга / Technical debt register

**Дата / Date:** 2026-08-30.

Приоритет / Priority: `P0` критическая проблема · `P1` исправить до развития · `P2` желательно · `P3` улучшение.

| ID | Component | Problem | Evidence | Impact | Complexity | Priority |
|---|---|---|---|---|---|---|
| TD-01 | Deployment | Живое устройство работает на до-переименовочном деплое (`~/nasa`, `Nasa_home.git`, `homecloud_*`), git — на переименованном (`nas_jetson_nano-*`) | `INVENTORY.md` §1; `docs/plans/tranquil-wandering-truffle.md` | Наивный деплой сломает `report_cmd`/`backup_cmd`, риск дублирования API-контейнера (уже частично снят — см. коммит `42ee9dd`) | HIGH (требует окна обслуживания) | **P1** — runbook уже готов (Часть B плана), не выполнен |
| TD-02 | Backup | Фото Immich (единственный актив семьи, ~6-9 ГБ) не покрыты offsite-бэкапом — только БД | `RISKS.md` R-01 | Безвозвратная потеря при отказе SSD | MEDIUM | **P0** — самый весомый пробел проекта по последствиям |
| TD-03 | Talk / coturn | `coturn` описан в compose, не развёрнут на VPS — видеозвонки вне LAN, вероятно, не проходят NAT | `INVENTORY.md` §15, `RISKS.md` R-03 | Функциональный пробел (не security) | LOW-MEDIUM (образ уже готов, нужен только деплой на VPS) | **P2** |
| TD-04 | `nasa-api` deps | Единственный из 3 Python-сервисов без пиннинга зависимостей (`>=`, не `==`), включая auth-библиотеки | `DEPENDENCIES.md` §2 | Непредсказуемый дрейф версий при `--build` | LOW (просто закрепить `==`) | **P2** |
| TD-05 | Docker images | ~13 dangling-образов (2.5-3 ГБ), плавающие теги `latest`/`release`/`apache` у 5 образов | `DEPENDENCIES.md` §3, §5 | Неконтролируемый апгрейд при следующем `pull`/`build`; лишнее место на SSD | LOW | **P3** |
| TD-06 | Logging | `jms583-health.log` без logrotate (единственный из проектных логов) | `RISKS.md` R-08 | Медленный, но неограниченный рост | LOW | **P3** |
| TD-07 | Storage preflight | `storage_preflight.sh` не проверяет явно свободное место перед стартом сервисов | `RISKS.md` R-09 | Тихий сбой при будущем заполнении диска | LOW | **P2** |
| TD-08 | API security | CORS `*` на порту 8099; JWT-секрет может не быть закреплён явно | `SECURITY.md` §3 | MEDIUM (эксплуатируемость низкая в LAN-периметре) | LOW | **P2** |
| TD-09 | SD-карта | Мониторинг износа SD (`nas_jetson_nano-sd-wear`) есть в git, не развёрнут на устройстве | `RISKS.md` R-12 | Отказ SD = полный простой хоста, сейчас без раннего предупреждения | LOW (юнит уже готов) | **P2** |
| TD-10 | Docs sync | README.md отстаёт от CLAUDE.md минимум на несколько дней/событий (детали — отдельная документационная проверка, см. запрос владельца от 2026-08-30) | `AUDIT_REPORT.md` §31 | Вводит в заблуждение нового читателя | LOW (ручная синхронизация) | **P2** |
| TD-11 | Platform EOL | Ubuntu 18.04.6 (EOL 04/2023) + Python 3.6.9 (EOL 12/2021) как база хоста | `DEPENDENCIES.md` §1 | HIGH при необнаруженной уязвимости в непропатченном пакете | HIGH (упирается в потолок JetPack для Nano — замена ОС фактически требует новой платы) | **P1** — не устранимо без замены оборудования, но требует явного решения (принять риск / ESM / план замены) |
| TD-12 | `.env.bak.*` | 10 версий бэкапа `.env` без ротации/удаления старых | `SECURITY.md` §5 | Растущая поверхность при компрометации диска | LOW | **P3** |
| TD-13 | `docker-compose.stage1.yml` | Устаревший монолитный compose-файл, использование не подтверждено ни в одну, ни в другую сторону | `INVENTORY.md` §2, `UNKNOWNS.md` | LOW — риск случайного запуска с конфликтом `container_name` | LOW (grep + решение удалить/оставить) | **P3** |
| TD-14 | Tests | Coverage maturity = 1 (единичные тесты); нет unit/integration-тестов на бизнес-логику FastAPI-сервисов (auth, редактирование PII, restart whitelist, OCS-парсинг) | (см. отчёт субагента «инвентаризация/архитектура», раздел 33) | Регрессии в этих зонах уже случались (задокументированы как «грабли» в CLAUDE.md), но не покрыты тестами кроме одного (SMART-парсинг) | MEDIUM | **P2** |
