name: homecloud-llm-gateway
services:
  llm-gateway:
    build:
      context: ../../services/llm-gateway
    container_name: homecloud_llm_gateway
    restart: always
    mem_limit: 256m
    ports:
      - "8090:8090"
    environment:
      LLM_PROVIDER: ${LLM_PROVIDER}
      DEEPSEEK_BASE_URL: ${DEEPSEEK_BASE_URL}
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY}
      DEEPSEEK_MODEL: ${DEEPSEEK_MODEL}
      DEEPSEEK_REASONER_MODEL: ${DEEPSEEK_REASONER_MODEL}
      # GigaChat / Sber — второй провайдер за тем же редактированием и бюджетом.
      GIGACHAT_AUTH_KEY: ${GIGACHAT_AUTH_KEY:-}
      GIGACHAT_SCOPE: ${GIGACHAT_SCOPE:-GIGACHAT_API_PERS}
      GIGACHAT_OAUTH_URL: ${GIGACHAT_OAUTH_URL:-https://ngw.devices.sberbank.ru:9443/api/v2/oauth}
      GIGACHAT_BASE_URL: ${GIGACHAT_BASE_URL:-https://api.giga.chat/v1}
      GIGACHAT_MODEL: ${GIGACHAT_MODEL:-GigaChat-2}
      GIGACHAT_IMAGE_MODEL: ${GIGACHAT_IMAGE_MODEL:-GigaChat-2-Max}
      GIGACHAT_CA_BUNDLE: ${GIGACHAT_CA_BUNDLE:-/certs/russian_trusted_bundle.pem}
      GIGACHAT_VERIFY_SSL: ${GIGACHAT_VERIFY_SSL:-true}
      LLM_GIGA_FALLBACK_DEEPSEEK: ${LLM_GIGA_FALLBACK_DEEPSEEK:-true}
      # Cloud.ru Foundation Models (optional)
      CLOUDRU_FM_API_KEY: ${CLOUDRU_FM_API_KEY:-}
      CLOUDRU_FM_BASE_URL: ${CLOUDRU_FM_BASE_URL:-https://foundation-models.api.cloud.ru/v1}
      CLOUDRU_FM_MODEL: ${CLOUDRU_FM_MODEL:-ai-sage/GigaChat3-10B-A1.8B}
      CLOUDRU_FM_MAX_TOKENS: ${CLOUDRU_FM_MAX_TOKENS:-2048}
      CLOUDRU_FM_TIMEOUT: ${CLOUDRU_FM_TIMEOUT:-120}
      LLM_DAILY_TOKEN_LIMIT: ${LLM_DAILY_TOKEN_LIMIT:-100000}
      LLM_MONTHLY_COST_LIMIT_USD: ${LLM_MONTHLY_COST_LIMIT_USD:-10}
      LLM_PRICE_USD_PER_MTOKEN: ${LLM_PRICE_USD_PER_MTOKEN:-0.5}
      LLM_USAGE_FILE: ${LLM_USAGE_FILE:-/data/llm_usage.json}
      LLM_REDACT_PERSONAL_DATA: ${LLM_REDACT_PERSONAL_DATA}
      LLM_REDACT_NAMES: ${LLM_REDACT_NAMES:-}
      LLM_USER_LIMITS: ${LLM_USER_LIMITS:-}
      LLM_USER_DAILY_TOKEN_LIMIT: ${LLM_USER_DAILY_TOKEN_LIMIT:-0}
      LLM_ALLOW_IMAGE_ANALYSIS: ${LLM_ALLOW_IMAGE_ANALYSIS:-false}
      # Локальная Ollama — dev only (ADR-0007: workstation not a prod node).
      # If URL set and LLM_PREFER_LOCAL=true, gateway may prefer it when reachable.
      LLM_LOCAL_URL: ${LLM_LOCAL_URL:-}
      LLM_LOCAL_MODEL: ${LLM_LOCAL_MODEL:-qwen3.5:4b}
      LLM_LOCAL_MAX_TOKENS: ${LLM_LOCAL_MAX_TOKENS:-512}
      LLM_LOCAL_TIMEOUT: ${LLM_LOCAL_TIMEOUT:-120}
      LLM_PREFER_LOCAL: ${LLM_PREFER_LOCAL:-false}
      LLM_LOG_PROMPTS: ${LLM_LOG_PROMPTS}
      LLM_LOG_RESPONSES: ${LLM_LOG_RESPONSES}
    extra_hosts:
      # Обратный SSH-туннель со станции слушает на хосте; из контейнера он виден
      # только по этому имени. Тот же приём, что у API-контейнера.
      - "host.docker.internal:host-gateway"
    volumes:
      # Persist budget counters so a restart does not reset the day's spend.
      - llm_usage:/data
      # Russian Trusted CA bundle — required for GigaChat TLS verification.
      # Public root certificates, not secrets; read-only.
      - ../../config/certs:/certs:ro
    healthcheck:
      test: ["CMD-SHELL", "python3 -c \"import urllib.request; urllib.request.urlopen('http://localhost:8090/health', timeout=3)\""]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    networks:
      - homecloud_internal

volumes:
  llm_usage:

networks:
  homecloud_internal:
    driver: bridge
