# CLAUDE.md — NAS_Jetson_Nano

> Этот файл читается Claude Code автоматически при открытии проекта.
> Содержит контекст, команды и правила для работы в этом репозитории.

## Проект

**NAS_Jetson_Nano** — приватный семейный облачный сервер на NVIDIA Jetson Nano 4 GB + USB SSD 250 ГБ + USB HDD 2 ТБ.
Заменяет Google Photos (→ Immich), Google Drive (→ Nextcloud), облачный NAS (→ Samba).

- GitHub: https://github.com/AlexeyBorovskoy/NAS_Jetson_Nano
- Owner: AlexeyBorovskoy (a.e.borovskoy@gmail.com)
- Текущий релиз: **v1.6.0** — сеть перестроена, алерты заговорили, один диагноз отозван
- Основная ветка: `main`

## Операционное состояние

**Состояние на 2026-08-25 ~08:45 UTC: VPN на VPS расширен и здоров — `amnezia-xray`/`amnezia-awg2` не перезапускались, пиров WireGuard стало 19 (было 13). Остальные подсистемы сегодня заново не проверялись — на уровень вчерашнего снимка (13 контейнеров up healthy, рестартов 0, бэкапы свежие 24.08 03:03, туннель Jetson↔VPS active, ZRAM 595 из 1982 МБ, Deco вторым роутером позади EC220, Jetson в гигабите).**
**Сделано 25.08:** Диагностирован и обойдён дефект генерации клиентов в приложении AmneziaVPN Desktop (`ErrorCode 305` / «SSH запрос отклонён»). Гипотеза владельца про смену IP VPS проверена и отклонена (чистый TCP до старого и нового IP — одинаково быстрый, ~50 мс). SSH-транспорт и авторизация на VPS полностью здоровы (проверено `sshd` `LogLevel DEBUG3`, снято безопасно через `reload`); сбой — в логике самого приложения, которое либо не доходит до `exec`, либо рвёт канал сразу после него, ни разу не записав нового пира. Обход: 6 клиентов добавлены вручную через `wg set` + правку `awg0.conf` (с бэкапом), в том числе один подтверждён владельцем рабочим на Vostro. Разбор: `docs/plans/CHECKPOINT_2026-08-25.md`.
**Сделано 24.08:** Волна 0 сдвинулась с мёртвой точки — постоянный туннель Vostro↔VPS (порт 10222) + отдельный админ-ключ, restic-таймер на Vostro **в бою** (фаза 1: только дампы БД), восстановление проверено взаправду (снэпшот `ab975984`, все `*.sql.gz` прошли `gzip -t`). Мимоходом починен непроверенный дефект от 22.08: ключ для забора дампов на VPS был авторизован без `port-forwarding` и путь не работал бы вовсе. Канал под алертом Фазы E. Пароль restic-репозитория — в Windows Credential Manager, не только в чате. **Отдельно найден и починен дефект `@бобик`:** рассинхронизация таймаутов (бот сдаётся за 60 с, шлюз терпит локальную модель 120 с) держала бота немым на личные вопросы владельца; первое исправление конфигом не подействовало, потому что `docker restart` не перечитывает `.env` — понадобилось пересоздание (`up -d`), подтверждено чтением переменной внутри самого процесса. Разбор: `docs/plans/CHECKPOINT_2026-08-24.md`.
**Сделано 23.08:** локальная модель выкачена в бой третьим провайдером (`@бобик` отвечает из дома, платная квота не тратится); `ClientAliveInterval 60` на Jetson — иначе слушатель обратного туннеля висел бы ~2 ч после выключения станции; журнал systemd переведён в постоянный режим.
**⚠️ Перезагрузка 17.08 была аппаратной, а не штатной.** `PMC reset source: TEGRA_POWER_ON_RESET`, паники в `ramoops` нет, парной строки `shutdown` в `wtmp` нет. Провал питания или PMIC-watchdog — **различить нечем**, и это записано как неизвестное, а не как удобная версия. Разбор: `docs/plans/CHECKPOINT_2026-08-23.md` §3.
**⚠️ Отзыв собственного диагноза (2026-08-22):** я объявил, что Talk-бот «12 дней бил в стену» — **это неверно**. Замер базовой линии: 13 626 опросов, **все `304`, ноль `400`**. Все 7 975 отказов уложились в один час — тот, в котором я снял `overwritehost` и вернул проверку доверенного домена, сломав бота, который ходит по имени `host.docker.internal`. Я диагностировал собственную регрессию как застарелый дефект. Подробности — в `docs/28`, раздел 3.3.
**Состояние на 2026-09-19:** полный аудит — `docs/audit/2026-09-19_full_audit/`; **единый план** — сводная редакция `docs/plans/DEVELOPMENT_PLAN_2026-09_SBER_ERA.md` (очередь A–F). Этап A в git закрыт: единая раскладка хоста (`scripts/lib/layout.sh`, `docs/35_HOST_LAYOUT.md`), шлюз починен (чат GigaChat был 500 после `d52c11b`), ворота и CI гоняют тесты сервисов, CI зелёный впервые с 30.08. **Этап A выкачен на Jetson 2026-09-19 04:37–04:45 UTC** (`DEPLOY_STAGE_A_2026-09.md`): репо `bdba26b`, `/etc/nas-layout.env` (prefix `nasa`), авто-восстановление SSD `Result=success`, шлюз и API пересобраны (чат GigaChat 200, 0.6 с), **сервисный токен шлюза включён** (без токена 401, бот шлёт токен), `config/.env` → 600, failed-юнитов нет; правило №13 — VPS до/после идентичен (19 пиров). **Ночью 19.09** утверждены (в git, устройство не трогалось) две спецификации по запросу сына, приоритет владельца (план, задача E9): домашняя качалка (aria2) и первый срез Telegram-бота — обращение по имени «@бобик», размер закачки без предела (SSD ≤20 ГБ / HDD больше), вопросы к GigaChat в этом же срезе, SOCKS-туннель на `172.17.0.1:1080` (Jetson не достаёт Telegram напрямую, замер 3 из 3 таймаут); реализация — следующий шаг. Разбор: `docs/plans/CHECKPOINT_2026-09-19.md`.

**Сделано 2026-09-22:**
- ✅ **P0 закрыт: вторая копия Immich на HDD живая.** Утверждение 21.09 «W2 не выкачен, фото только на SSD» **отозвано по замеру**. Таймер `nas_jetson_nano-immich-hdd-copy` active; последний прогон 2026-09-22 04:22 UTC SUCCESS; `/mnt/storage/immich` = `/mnt/hdd2tb/backups/immich` ≈13 G; sha256 3/3. Off-site S3 по-прежнему пуст (решение владельца). Точка: `docs/plans/CHECKPOINT_2026-09-22.md`.

**Сделано 2026-09-21:**
- 🔴 **Off-site копия фотографий удалена** по решению владельца — 462 объекта / 7.50 ГБ, корзина пуста. Тогда записали «P0 снова открыт» и «W2 не выкачен» — **вторая фраза неверна**, закрыто замером 22.09 (L1 HDD). L2 S3 не восстанавливали.
- ✅ **Починен денежный алерт Cloud.ru (E5) — со второй попытки, первая была неверной.** Семантика полей доказана замером, 13 строк из 13: `usefact` — количество, `cost` — **ставка** за единицу, `amount` — **уже начислено в рублях** (= `usefact × cost`), `amount_nds` = `amount × 1.22`. Деньги — это сумма `amount`, ничего умножать не надо. Решающий довод — НДС лежит на `amount`, а на количество НДС не начисляют. Три прочтения одной выгрузки за 01.08–21.09: сумма `cost` = 953.34 ₽ (сложение ставок), сумма `amount × cost` = 7.72 ₽ (деньги на ставку второй раз — **эту версию я успел выкатить и закрепить тестом**), сумма `amount` = **0.0263 ₽** (0.0321 с НДС) — верно. То есть за полтора месяца израсходовано **две с половиной копейки**. Первую ошибку нашёл сосед (`m0138`), свою же формулу отозвал через час (`m0141`); обе проверены здесь независимо. Признак, который был у меня перед глазами: `amount / usefact` у токенов даёт ровно 466.67 — ставку, значит `amount` уже произведение. Тесты — `tests/unit/test_cloudru_cost_is_a_rate.py` (12 проверок, включая сами данные).
- 🟠 **Открытый вопрос (`m0142`): что значит `usefact` для Object Storage.** В день, когда в корзине лежало 7.5 ГБ, биллинг показал 0.0656 и 0.1250 ГБ — **расхождение в 60 раз**. Для токенов равенство `amount = usefact × cost` проверено, для хранилища проверить нечем: там `amount = 0`, ноль умножается на что угодно. Значит сопоставление `FREE_TIER_LIMITS` по хранилищу **не проверено**, и «вычитаем 15 ГБ по `usefact`» может дать ложное спокойствие. Записано как неизвестное.
- ✅ **Cloud.ru открыт соседнему проекту** — карта без секретов `E:\agent_coordination\shared\nas\CLOUDRU_FULL_ACCESS_2026-09-21.md` (`m0135`). Выяснено (`m0137`): организация **общая**, тот же `customerId`, но Object Storage сосед занимать **не будет**; общий дефицит — деньги (99.97 ₽ + 4000 бонусов), не гигабайты.
- ⚠️ **Аптайм Jetson — 2 д 16 ч** (замер 2026-09-21 06:55 UTC), то есть устройство перезагружалось около **18.09**. В таблице ниже значится «аптайм с 2026-08-17» — расхождение реальное, причина не выяснялась.

**Сделано 2026-09-20 (большой день, выкат и закрытие P0):**
- ✅ **Off-site копия фотографий Immich** — Object Storage Cloud.ru, восстановление проверено сверкой sha256 (20/20). Подробности — в блоке о главном долге выше.
- ✅ **D2 выкачен** — алерт после аварийной загрузки: `nas_jetson_nano-boot-alert.service` включён в автозапуск, канал до Telegram проверен прогоном `--test` (сообщение доставлено). Юнита в git не было — агент оборвался лимитом до его создания, юнит дописан отдельно.
- ✅ **E3 выкачен** — `@бобик` отвечает владельцу на «что сломалось?». Проверка `/mnt/hdd2tb` идёт в отдельном потоке с таймаутом: повторение инцидента с зависшим ntfs-3g бот переживёт и ответит «HDD не отвечает», а не зависнет. Членам семьи — вежливый отказ **до** сбора данных.
- ✅ **E5 в git** — алерт расходов Cloud.ru. API не отдаёт остаток бесплатного тарифа, поэтому две независимые тревоги: безусловная на любой денежный расход и расчётная на приближение к лимиту.
- 🔴 **Инцидент с зависанием `/mnt/hdd2tb`** (два параллельных обхода ntfs-3g от субагента): легли NAS API и Samba, восстановление потребовало умерщвления демона и пересоздания четырёх контейнеров. Разбор и два новых правила — в «Граблях».
- 🔴 **Отозван диагноз «Cloud.ru API возвращает 503»**: запросы шли на несуществующее имя хоста, а `503` у этой платформы означает «не понял запрос». Настоящая карта API — `docs/integrations/sber/CLOUD_RU.md`. Задания живут в `/v2/jobs`, встроенного расписания у них нет.
- **Голосовые сообщения (E10):** движок выбран владельцем — **Vosk small-ru** (WER 17.07 % против 62.20 % у whisper.cpp tiny на замере с Jetson). Спецификация — `docs/superpowers/specs/2026-09-20-voice-messages-design.md`.
- **Не завершено:** этап H (инвентаризация фотоархива) оборван лимитом; решение D4 по Immich ML ждёт владельца — единственный блокер административный, включить гипервизор на станции.

**Вечер 2026-09-19 (в git, НЕ на устройстве — ждёт «деплой»):** **E2** — алерт квоты GigaChat по моделям (`talk-alert` читает `/var/lib/nas-giga-balance/balance.json`); **D3** — внешний сторож `services/watchdog/` (Cloud.ru Job → VPS `naswatch` с ключом на одну команду → Telegram владельцу), 44 теста, runbook `docs/plans/DEPLOY_D3_WATCHDOG_2026-09.md` (сначала спайк в Cloud.ru). **Решения владельца:** S3 отложен (off-site — Vostro), ИБП отложен, копии архива 1,4 ТБ нет и не будет (риск принят), Immich ML в Cloud.ru — только в free tier. Точка: `docs/plans/CHECKPOINT_2026-09-19.md` (раздел «Вечер»).

**Текущая точка: `docs/plans/CHECKPOINT_2026-09-22.md`** (P0 L1 HDD закрыт замером). Предыдущая: `CHECKPOINT_2026-09-21.md` (S3 очищен, биллинг, Cloud.ru соседу). Предыдущая по домашней сети: **`docs/plans/CHECKPOINT_2026-09-12.md`** (домашняя сеть: mesh перестроен, прошивка Deco сделана наполовину, 4 гипотезы отозваны); предыдущие — `CHECKPOINT_2026-09-11.md`, `CHECKPOINT_2026-08-30.md`. Слепок сети: **`docs/34_NETWORK_SNAPSHOT_2026-09-12.md`** (перестройка mesh, замеры до/после, семь ловушек); предыдущий — `docs/28_NETWORK_SNAPSHOT_2026-08-22.md`. Предыдущая точка: `docs/plans/CHECKPOINT_2026-08-25.md`.

**Сделано 30.08 (день аудита, устройство и VPS не менялись — всё read-only):**
- **Полный технический аудит** — `docs/audit/` (12 файлов + `audit_summary.json`). Оценки 0–5 по 11 категориям, реестры рисков и техдолга, 4 сценария развития. Главное: **P0 — фото Immich без off-site**; P1 — миграция устройства и EOL Ubuntu 18.04/Python 3.6.9; `coturn` описан в compose, но **никогда не разворачивался на VPS**; CORS `*` на API :8099; GPU/CUDA/TensorRT установлены, но **не используются ничем** (`GR3D_FREQ 0%`). Восстановление с нуля — **PARTIALLY**.
- **Расхождение git↔устройство, часть A закрыта:** 3 прод-фикса с устройства (шара `hdd2tb`, bind-mount `/mnt/hdd2tb`, поднятые `mem_limit`) перенесены в git — иначе пропали бы молча. Закрыт риск дублирования: compose переименовывал живой `homecloud_nasa_api`, наивный деплой поднял бы **второй контейнер Talk-бота** на занятом порту 8099 — откачено по решению владельца. Часть B (физический переезд) — runbook готов, окно не назначено.
- **Документация:** 5 документов описывали off-site как несделанный (фаза 1 в бою с 24.08) — исправлены; **50 документов доведены до RU+EN** по правилу №15. Проверено пословно: ни одно русское слово не потеряно.
- **`check_no_secrets.sh` ломал каждый коммит** — ловил `RESTIC_PASSWORD_FILE=/root/...`, то есть **путь** к секрету, а не значение. Добавлено узкое исключение для `*_FILE`/`*_PATH`; проверено, что настоящий секрет по-прежнему ловится.
- **AmneziaVPN 5.0.1.5** (21.08) — разведка: ни в одном changelog нет упоминаний SSH-провижининга, оба похожих issue (#2011, #2837) **открыты без ответа мейнтейнеров**. Обновление даёт security-фикс, но **на починку нашего дефекта рассчитывать нельзя** — ручной обход через `wg set` остаётся рабочим путём.
- **Инструкция для семьи** (Ваня/Оля/Уля: Talk + Immich + VPN) — `docs/local/domashnee-oblako-instrukciya.html`, **намеренно вне git**: репозиторий публичный, а документ содержит логины членов семьи.
- ⚠️ **Найдено расхождение:** здесь записан логин админа Immich `admin@nas_jetson_nano.local`, **на устройстве реально `admin@nasa.local`**. Не исправлено, зафиксировано в точке 30.08.

> 🔴 **Обязательное условие всех работ (владелец, 2026-08-22):** VPN на VPS сохраняет
> работоспособность, конечные пользователи не затрагиваются. Проверять **до и после**
> каждого изменения: контейнеры `amnezia-*` не перезапускались, наружу слушают только
> 22/443/40568, число пиров не уменьшилось (`docker exec amnezia-awg2 wg show`).
> nginx править только через `nginx -s reload`, не пересозданием контейнера.

> ✅ **P0 (вторая копия Immich вне SSD) ЗАКРЫТ 2026-09-22 на L1 HDD.** Замер с Jetson:
> таймер W2 active, прогон 04:22 UTC SUCCESS, 13 G ≈ 13 G, sha256 3/3.
> 🔴 **L2 S3 по-прежнему пуст** (удаление 21.09, решение владельца). Это не тот же P0:
> «один диск SSD» закрыт; «нет off-site» остаётся принятым риском до нового S3.
> Корзина `nas-immich-offsite` очищена 21.09: **462 объекта, 7.50 ГБ**.
> Утверждение 21.09 «W2 не выкачен» — **отозвано**.
> ⚠️ Причина, названная при постановке задачи (освободить место под соседний проект), **не подтвердилась**:
> `work` ответил на доске (`m0137`), что Object Storage ему нужно **ноль**, и что общий счёт
> упирается не в гигабайты, а в деньги (на балансе 99.97 ₽ + 4000 бонусов). Владелец подтвердил
> удаление после того, как это стало известно.
> 🔴 **Попутная находка, не закрытая:** `restic` **с устройства не открывает** этот репозиторий —
> `Stat(<config/>) failed: Stat: 400 Bad Request`, 17 повторов, `Fatal: unable to open config file`.
> При этом прямой S3 подписью SigV4 с того же устройства и того же ключа работает (опись и удаление
> прошли без единого отказа), а `AWS_ACCESS_KEY_ID` в `config/.env` **в правильной форме**
> `<tenant>:<keyId>` (36+32 симв.). Значит вчерашняя проверка восстановления шла **не с Jetson**,
> и путь «SSD умер → достаём фотографии из облака» **не был проверен на устройстве**. Причина 400
> не установлена (регион? path-style? версия minio-go?) — записано как неизвестное. Это важно
> для любой будущей off-site копии: проверять восстановление **с той машины, которая будет восстанавливать**.
>
> <details><summary>Прежняя запись (2026-09-20, отозвана 2026-09-21 — долг был закрыт и снова открыт)</summary>
>
> ✅ **ГЛАВНЫЙ ДОЛГ ЗАКРЫТ 2026-09-20: у фотографий Immich есть off-site копия.**
> Object Storage Cloud.ru (`s3.cloud.ru`, корзина `nas-immich-offsite`, репозиторий `restic`
> в подпапке `immich`). Залито 7250 файлов / 7.94 ГБ за 32 минуты, в хранилище 7.5 ГБ —
> внутри бесплатных 15 ГБ. **Восстановление проверено по-настоящему**: 20 случайных снимков
> возвращены из облака и сверены с оригиналами по sha256 — 20 из 20 совпали.
> В копию входят оригиналы (`upload`) и дампы БД Immich (`backups`); `encoded-video` и
> `thumbs` исключены осознанно — Immich строит их заново, а они съели бы треть лимита.
> 🔴 Пароль репозитория: Windows Credential Manager `nas-jetson-restic-cloudru-s3`
> **и** `RESTIC_CLOUDRU_PASSWORD` в `config/.env` на устройстве. Без него данные не вернуть.
> ⚠️ Архив 1.4 ТБ на HDD в облако **не поедет** — решение владельца, риск принят.
> Автоматического расписания у этой копии пока НЕТ: снимок сделан вручную.
>
> </details>

> 🟠 **Прежняя запись о долге (актуальна только для проверки авто-восстановления SSD).** Off-site бэкап **дампов БД теперь реально
> работает**: restic-репозиторий на Vostro, ночной таймер, восстановление проверено
> (2026-08-24). Но это только фаза 1 — фотографии Immich (~6 ГБ, весь каталог 8.9 ГБ)
> по-прежнему лежат только дома, фаза 2 отдельным решением. Проверка авто-восстановления
> реальным отвалом SSD — тоже не сделана. Разбор: `docs/plans/WAVE_0_OFFSITE_BACKUP.md`,
> `docs/plans/CHECKPOINT_2026-08-24.md`.

> ⚠️ **Расхождение git ↔ устройство.** Живой Jetson работает на СТАРОМ, до-переименовочном деплое. Rename `NASA → NAS_Jetson_Nano` сделан только в git — **на устройство не выкатан**. Фактически на устройстве:
> - Репо: **`~/nasa`**, remote `github.com/AlexeyBorovskoy/Nasa_home.git` (HEAD **`bdba26b`**, выкат этапа A 2026-09-19; юниты и каталоги по-прежнему `nasa-*` — код находит их через раскладку `/etc/nas-layout.env`). **`git pull` на устройстве — только по runbook'у деплоя** (A5). Копии скриптов в `/usr/local/sbin/nasa-*` — старые версии, работают; переустановка — при Part B
> - Контейнеры: префикс **`homecloud_*`** (не `nas_jetson_nano_*`), API-контейнер `homecloud_nasa_api`
> - systemd-юниты: префикс **`nasa-*`** (не `nas_jetson_nano-*`)
> - Логи мониторинга: **`/var/log/nasa-monitor/`**
> - Сетевой профиль NetworkManager: **`nasa-lan`** (правило №3 ниже называет его по будущему имени)
> - На устройстве **более 10 файлов изменены вручную** и не в git (Talk-бот, LLM-шлюз, HDD 2 ТБ, Samba, скрипты отчётов, compose, `.env`, `config/certs/`) — рядом лежат бэкапы `*.bak.*` и `*.bak.giga.1786366985`. Любой `git pull` на устройстве их затрёт.
> **Обновлено 2026-08-30:** построчная сверка всех 13 изменённых на устройстве файлов с текущим git завершена. Найденные реальные прод-фиксы (шара `hdd2tb` в Samba, `/mnt/hdd2tb` в Nextcloud/Samba, увеличенные `mem_limit` netdata/uptime-kuma) перенесены в git — **содержательных потерь при будущем деплое больше нет**, git теперь надмножество устройства. Отдельно закрыт риск: git переименовывал реально работающий контейнер `homecloud_nasa_api` → `homecloud_nas_jetson_nano_api` (конфликт порта 8099 при наивном деплое) — **решено не переименовывать контейнер**, откачено обратно. Остался только физический переезд устройства (каталог, systemd-юниты, логи) — runbook готов, ждёт отдельного окна обслуживания (см. ниже).
> Полная миграция устройства на переименованный layout — отдельная неначатая задача (backup + окно обслуживания).

> ✅ **Закрыто с прошлого аудита (2026-08-01 → 2026-08-10):**
> 1. **Сервисы больше не открыты в интернет.** ufw на VPS пускает 8080/8443/2283/2443/8090/9443/8099/8091/8765/8766 только с `172.29.172.0/24` и `10.8.1.0/24`. Наружу открыты лишь 22, 443 (XRay) и 40568/udp (AmneziaWG). Правило №4 соблюдено.
> 2. **Бэкапы починены.** Причина простоя 24.07 → 09.08 — незакавыченное `TALK_BOT_DISPLAY_NAME=NAS Bot` в `config/.env`: `source` под `set -euo pipefail` падал с кодом 127. Роняло разом бэкапы, `storage_preflight.sh`, `nasa-ssd-recovery` и `jetson-nas-health`. Исправлено 2026-08-09; **restore впервые проверен** накатом дампов во временную БД.
> 3. **Сменился IP VPS.** `193.8.215.130` заблокирован российскими ISP (per-IP stateful-блок, с 2026-08-06). Рабочий адрес — **`95.163.176.103`**. `VPS_HOST` живёт в root-owned `/opt/nasa/config/.env` (это НЕ `~/nasa/config/.env`).

| Компонент | Статус | Детали |
|---|---|---|
| Jetson Nano | ✅ up | 192.168.0.50, eth0 1000 Мбит/с Full, аптайм с **2026-08-17 11:05 UTC** (перезагрузка аппаратная, причина не установлена — см. точку 23.08 §3.1) |
| Температуры | ✅ норма | CPU/AO 45 °C, GPU 41.5 °C, PMIC 50 °C |
| RAM | 🟠 плотно | 2.0 / 3.9 ГБ занято, доступно ~1.6 ГБ |
| ZRAM | ✅ работает, тюнить нечего | 4 × 495 МБ = 1.98 ГБ, **lzo**, сжатие **2.27×**, занято 33 %, трафик ~6 МБ/час. ⚠️ `lz4`/`zstd` в ядро 4.9-tegra **не собраны** — решение соседей с `zstd` неприменимо. `vm.page-cluster` → 0 (`/etc/sysctl.d/60-nasa-zram.conf`). Заполнение подкачки теперь под алертом. Разбор — `docs/33_ZRAM.md` |
| SSD `/dev/sda1` → `/mnt/storage` | ✅ mounted | 229G, **5 %** (9.7G), ext4 noatime |
| HDD 2 ТБ `/dev/sdb1` → `/mnt/hdd2tb` | ✅ mounted | WD20EADS, **NTFS** через ntfs-3g, метка `Borovskoy_Hard`, 1.9T, **76 %** (1.4T архива, 462G свободно) |
| USB SSD энклоужер | ✅ **JMS583** (152d:a583) | USB 3.0 SuperSpeed 5000 Mbps, драйвер `usb-storage` |
| USB HDD мост | ✅ **RTL9201** (0bda:9201) | USB 3.0, драйвер `usb-storage` |
| `usbcore.autosuspend=-1` | ✅ kernel | `/proc/cmdline` |
| `usb-storage.quirks=0bda:9210:rw,152d:a583:u,0bda:9201:u` | ✅ active | UAS отключён для **обоих** дисков. SSD write 250 / read 172 МБ/с; HDD read 106 / write 92 МБ/с |
| USB-ошибки | ✅ **0 в работе** | За 6 суток работы — ни одной. ⚠️ Прежняя запись «0» была неточной: при холодной загрузке 17.08 их было **4**, все в первые 17 с — 2-ТБ HDD не поднялся с первой попытки (`error=-110`, `I/O error on sdb sector 0`, `error -71`) и переподключился сам. Замер: `dmesg \| grep -E 'error -71\|uas_eh\|I/O error'` + отсечка по времени с момента старта |
| SCSI timeout | ✅ 120s | udev правило активно |
| USB watchdog / pre-boot / error monitor | ✅ active | `nasa-usb-watchdog.timer`, `nasa-usb-preboot.service`, `nasa-usb-monitor.service` |
| JMS583 health timer | ✅ active (waiting) | `nasa-jms583-health.timer` ежечасно; последний прогон `errors=0 warnings=0` |
| SSD hotplug auto-recovery | ✅ **восстановлен 2026-09-19** | Был сломан 2026-09-08…19 (код 127: захардкоженный `/home/admin/nas_jetson_nano/...`). После выката этапа A пути берутся из `scripts/lib/layout.sh` / `/etc/nas-layout.env`; ручной прогон юнита — `Result=success`, preflight `errors=0`. Тест выдёргиванием SSD — не делался |
| smartd | ⛔ **disabled by design** | Относится **только к SSD**: quirk `152d:a583:u` → usb-storage BOT → passthrough закрыт. Здоровье SSD закрывают JMS583-таймер и USB-монитор |
| SMART на HDD 2 ТБ | ✅ **работает** (перезамер 2026-08-23) | ⚠️ Прежде в проекте значилось, что SMART невозможен на **обоих** дисках — вывод был перенесён с моста JMS583 на RTL9201 без проверки. `smartctl -d sat /dev/sdb`: **PASSED**, наработка **2582 ч**, переназначенных/ожидающих/некорректируемых секторов — **0**, CRC-ошибок 0, самотест без ошибок. ⚠️ Уточнение: `smartctl -H` отвечает `SMART Status not supported: Incomplete response`, вердикт PASSED выносится **по атрибутам**, а не командой статуса |
| `nasa-hdd2tb-selftest.timer` | ✅ active | короткий SMART-самотест HDD еженедельно (пн 04:30). Длинный — вручную: `smartctl -t long -d sat /dev/sdb`, 490 мин |
| NAS API / шлюз (этап C) | ✅ **с 2026-09-19** | Всё, кроме `/healthcheck` и входа, требует JWT; роли: семья (статус) / владелец (`admin` + `API_OWNERS`: логи, Talk, действия); CORS выкл. Шлюз от UID 10001, токен обязателен, откат на DeepSeek только 429/5xx. Portainer только `127.0.0.1` (`ssh -L 9443:127.0.0.1:9443`), rpcbind выкл. |
| Бэкап конфигурации (restic, L1c) | ✅ **с 2026-09-19** | `nas_jetson_nano-config-backup.timer` 03:40: `.env`, `/opt/nasa/config/.env`, `/etc/nasa-monitor`, `config.php` NC, файлы NC, Samba users, дампы → шифр. репо `/mnt/hdd2tb/backups/restic-config`; 7д/4н/6м. Учения `nas_jetson_nano-restore-drill.timer` 1-го числа — первое **DRILL OK** 2026-09-19. 🔴 Пароль репо: `/root/.config/nas-backup/hdd.pass` + **Windows Credential Manager** (`nas-jetson-restic-config-hdd`). `backups/` смонтирован **ro** в Samba и Nextcloud |
| Бэкапы БД | ✅ работают | `nasa-backup.timer`, последний дамп **2026-08-23 03:05** (immich 19.9 МБ + nextcloud 2.68 МБ), каталог 154 МБ. 🔴 **Всё в одном доме** — off-site не сделан |
| Журнал systemd | ✅ **постоянный** (с 2026-08-23) | Прежде `Storage=auto` без `/var/log/journal` = журнал в tmpfs, и `--list-boots` знал ровно одну загрузку. Причину сброса 17.08 удалось восстановить только по `ramoops`, `wtmp` и ротированному `syslog` — везением, а не диагностикой. Теперь `Storage=persistent`, `SystemMaxUse=200M`, хранение месяц (корень — SD-карта, потому предел жёсткий) |
| Restore | ✅ **проверен 2026-08-09** | Накат во временную БД: NC 153 таблицы (users 5 = live, filecache 403 = live); Immich 61 таблица (asset 7098 = live, album 23 = live) |
| VPS reverse tunnel | ✅ active | `nasa-tunnel.service`, ESTAB `192.168.0.50 → 95.163.176.103:22`; на VPS подняты 18080/12283/18090/18099/10022/45876 |
| Docker daemon | ✅ active | **13 контейнеров** `homecloud_*` up (healthy), **restarts=0**, **OOMKilled=false** у всех |
| Talk-бот (Phase A) | ✅ running | `homecloud_nasa_api`, триггер `нас` — считается **дома**, наружу ничего не уходит |
| Talk-бот (Phase C) | ✅ **выкачено 2026-08-10** | Позывной **`@бобик`** → свободные вопросы наружу через redaction-шлюз. **6 комнат**, у каждого своя |
| Личные комнаты Talk | ✅ созданы (токены — `docs/local/IDENTIFIERS.md`, вне git; C7 2026-09-19) | `<room:owner>` Алексей · `<room:olga>` olga · `<room:ivan>` ivan · `<room:ulyana>` ulyana · `<room:anna>` anna · `<room:family>` общая |
| LLM Gateway | ✅ **3 провайдера** (2026-08-23) | **DeepSeek** (по умолчанию) + **GigaChat**. Оба — через одно редактирование и один бюджет. Провайдер выбирается полем `provider` в запросе |
| Локальная модель | ✅ **работает, вопрос не покидает дом** | Ollama `qwen3.5:4b` на рабочей станции через обратный туннель. `LLM_PREFER_LOCAL=true`: станция отвечает → идём к ней, выключена → DeepSeek как раньше. Локальные вызовы **не тратят платную квоту** (в учёте `calls` растёт, `tokens=0`). Проверено сквозным путём: `@бобик` → «🐕 Париж», токенов +0 |
| Персональный учёт токенов | ✅ работает | Лимит на человека, на дом (сутки) и на месяц по деньгам. Проверка **до** вызова, `429` при превышении. `GET /v1/usage` — разбивка по людям |
| Редактирование перед отправкой | ✅ 11 имён | почта, телефоны, токены, приватные ключи, `@упоминания`, домашние пути, **имена со склонениями**. `names_dropped` в `/health` покажет пропущенное |
| Картинки (Kandinsky) | 🟠 **только генерация** | `/v1/image/generate` работает. `/v1/image/edit` закрыт флагом (ниже). ⛔ **Реставрация/ретушь невозможны** — API не редактирует фото, см. `docs/plans/PHOTO_PROCESSING_FEASIBILITY.md` |
| `LLM_ALLOW_IMAGE_ANALYSIS` | ✅ **false** (с 2026-08-11) | Возвращён в `false`: провайдер фото всё равно не редактирует, а отправка наружу — реальный риск. `/v1/image/edit` отвечает `403`, бот говорит «фото не покидают дом». Снимать флаг только сознательно |
| Immich | ✅ v2.7.5 | **7098 ассетов** (6686 фото + 412 видео), 23 альбома, upload 5.8 ГБ (весь каталог 8.9 ГБ) |
| Nextcloud | ✅ v33.0.4 | `maintenance:false`, 5 пользователей, 403 файла в кэше |
| Nextcloud external storage | ✅ `/HDD-2TB` | mount id 1, Local → `/mnt/hdd2tb`, applicable All, sharing on, `files_external` 1.25.1 |
| Samba | ✅ up (**починена 2026-09-12**) | Контейнер `homecloud_samba`: `public`, `nextcloud`, `immich`, **`hdd2tb`**; пользователь `nas`, guest запрещён. 🔴 Рудиментный **хостовой** smbd вне проекта перехватывал порт 445 и отдавал три пустые шары — остановлен и снят с автозапуска (`systemctl disable --now smbd nmbd`). Подключение с Windows: `New-SmbMapping`, а не `net use` |
| LLM Gateway | ✅ healthy | `/health` → `{"status":"ok","provider":"deepseek","redaction":"true"}` |
| Beszel Agent Jetson (45876) | ✅ active | **systemd-юнит** `beszel-agent.service` (не контейнер), enabled |
| Beszel Hub (VPS:8091) | ✅ up | доступен только через VPN |
| DNS-имя VPS | ✅ **`borovskoy.dynv6.net`** (с 2026-08-12) | A → **`95.163.176.103`**, TTL 60, AAAA намеренно НЕ создана (IPv6 на VPS наружу не отвечает). dynv6, адрес статический — DDNS-клиент не нужен. ⚠️ Апдейтер, запущенный НА VPS, прописал бы **заблокированный** `193.8.215.130`: наружу машина выходит через него. Разбор — `DNS/VPS_DNS_BASELINE.md` (вне git, там же токен) |
| HTTPS на имя | ⛔ обычным путём невозможно | Порт **443 занят `amnezia-xray`**, порт 80 закрыт в ufw. Значит HTTP-01 и TLS-ALPN-01 отпадают. Единственный безопасный путь — **DNS-01 через API dynv6**, он не требует открытых портов. Освобождать 443 нельзя — правило №2 |
| VPS nginx | ✅ live | `nasa_nginx` up 5 недель; проксирует 8080/2283/8090/8099/8091 на порты туннеля |
| Telegram daily report | ✅ доставляется | `nasa-daily-report-telegram.timer` активен, последняя доставка 2026-08-09 16:33 MSK |
| Android apps | ✅ установлены | Immich + Nextcloud из Play Store, DAVx⁵ APK v4.5.14 |
| Immich Android | ✅ настроен | Логин: admin@nas_jetson_nano.local; снаружи — только через VPN |
| Keenetic Omni KN-1410 | ⛔ **план закрыт 2026-08-10** | Заменён Deco E4; остаётся холодным резервом, в сеть не вводится |
| TP-Link Deco E4 ×2 | ✅ **mesh перестроен 2026-09-12** | Наклейка: **Deco E4, EU/4.0** (веб-интерфейс пишет `E4R(4.0)`). Узлы `hallway` (главный, WAN `192.168.0.107`, LAN `192.168.68.1/22`) и `living_room` (**адрес плавает**: `.250` → `.249` после перезагрузки). Второй узел **перенесён ближе** — отклик 746 → **53 мс**, отказы 4 из 10 → **0**; станция 36 % / 175 Мбит/с → **85 % / 866.7 Мбит/с**. Прошивка **1.0.0 (2023)**, цепочка апгрейда `1.2.0 → 1.2.2 → 1.3.1` (прямой прыжок отвергается, `RSA2048_up`). Двойной NAT сохранён, шаги 2/3/6 регламента (`docs/27_HOME_NETWORK_MESH.md`) по-прежнему не выполнялись. Слепок — `docs/34_NETWORK_SNAPSHOT_2026-09-12.md` |
| EC220-G5 | ✅ **остался шлюзом** | `192.168.0.1`, MAC `<MAC:ec220>`. Jetson включён в него и **сохранил 1000 Мбит/с** — цена перестройки из регламента ещё не заплачена. **Не сбрасывать** — путь отката |
| Двойной NAT | 🟠 цена текущей схемы | Телефоны и станция в `192.168.68.0/**22**` достают NAS **только по IP**; Jetson не может инициировать соединение к ним; гостевой изоляции по-прежнему нет |
| Wi-Fi на EC220 | 🟠 **конкурирует с mesh** (замер 2026-09-12) | Старый роутер продолжает раздавать свою сеть, в `192.168.0.0/24` живут `.100` и `.102` с рандомизированными MAC. Два независимых Wi-Fi в одной квартире делят эфир; решение (погасить либо развести каналы) не принято |
| Скорости дома | ✅ замер 2026-09-12 | Провод Jetson → интернет **87–92 Мбит/с**; Wi-Fi **100** (после апгрейда главного узла Deco; до него 64–80); через AmneziaVPN **46–53** + эпизодический срыв до 0.8. DNS через Deco **7 мс** (было 73 на старой прошивке), через EC220 **5 мс**. Удержание сессии 20 мин: и через VPN, и мимо — **обе дожили**, обрывов нет |
| Прошивка Deco | 🟠 **сделана наполовину** | `hallway` **1.2.0** (2025-07), `living_room` **1.0.0** (2023-03). Веб-интерфейс обновляет **только главный узел**, облако отвечает отказом, канал до TP-Link при этом исправен. Остался один путь: удалить узел в приложении и добавить заново. **До этого не менять имя сети и пароль Wi-Fi** — конфигурация может не дойти до узла |

**🔑 Ротация паролей (2026-06-28):** Все пароли изменены на новые. Git history очищен (filter-repo). В git секретов нет; источник истины на устройстве — `~/nasa/config/.env`.

**🔧 Если SSD упал — восстановление АВТОМАТИЧЕСКОЕ:**
> Просто физически переткни кабель SSD. Система сделает остальное сама:
> udev(`sda1`) → `nasa-ssd-recovery.service` → mount → preflight → Docker → все 13 контейнеров
> Лог: `journalctl -u nasa-ssd-recovery` или `/var/log/nasa-monitor/ssd-recovery.log`
>
> Если авто-recovery не сработал (маловероятно; с 2026-09-19 снова рабочий, проверен ручным прогоном):
> `sudo -S systemctl start nasa-ssd-recovery.service`

**⚠️ Грабли, проверенные на практике:**
- **OCS API Nextcloud принимает форму, а не JSON.** `_ocs_post` слал `json=` и получал
  `404 / statuscode 998 "Invalid query"`. Из-за этого Talk-бот **не отвечал никому с
  момента запуска Фазы A** — в статусе копилось `processed`, а `replied` оставался 0,
  и ошибка глушилась в `except`. Отправлять `data={...}`. Тот же дефект вероятен
  у других вызывающих сторон `_ocs_post` — не проверено.
- **Код запекается в образ** (`COPY app /app/app`). Правка файла на хосте + `up -d
  --force-recreate` не даёт ничего — контейнер поднимется со старым кодом. Нужен `--build`.
- **Не передавать два base64-файла одной ssh-командой** — склейка через разделитель
  бьёт содержимое, контейнер потом падает с `Attribute "app" not found`. По одному файлу.
- **Картинки принимает только `GigaChat-2-Max`.** Базовая `GigaChat` отвечает
  `422 Model does not support image` — причём файл до этого успешно загружается,
  поэтому ошибка всплывает поздно и выглядит непонятно.
- **GigaChat API не редактирует фотографии** — только смотрит и генерирует новое по
  описанию. Реставрация и ретушь через него невозможны, см.
  `docs/plans/PHOTO_PROCESSING_FEASIBILITY.md`.
- **Фильтр имён — список слов, а не модель.** Уменьшительные (Оля, Ваня, Аня) — другая
  основа и автоматически не выводятся; перечислять все формы. Короткие основы однажды
  молча отбрасывались — теперь видно в `names_dropped` у `/health`.
- В `config/.env` **любое значение с пробелом обязано быть в кавычках** — иначе `source` под `set -e` молча роняет бэкапы, preflight, ssd-recovery и health-чек, а systemd при этом рапортует `success`. Проверять свежестью файлов в `/mnt/storage/backups/database-dumps/`, а не статусом юнита.
- `echo "$P" | sudo -S <cmd>` **нельзя** сочетать с heredoc/stdin — `sudo -S` забирает stdin, и в файл уходит пароль. Писать файл в `/tmp` от `admin`, затем `sudo cp`.
- После правки `/etc/fstab` обязателен `systemctl daemon-reload`, иначе systemd примонтирует по старому сгенерированному юниту.
- FUSE + Docker: если хостовый mount `/mnt/hdd2tb` пересоздан, контейнеры держат мёртвую ссылку → нужен `docker compose up -d --force-recreate`.
- 🔴 **Два параллельных рекурсивных обхода по `ntfs-3g` вешают точку монтирования целиком.** Поймано
  2026-09-20: субагент инвентаризации запустил два `find` по `/mnt/hdd2tb` одновременно (вопреки прямому
  запрету) — демон `ntfs-3g` перестал обслуживать запросы, оба `find` зависли **внутри** его вызовов и
  **не умирали от `kill -9`** (сигнал принимается, `rc=0`, процесс остаётся). Заодно лёг NAS API: контейнер
  держит `/mnt/hdd2tb/Downloads` и упёрся в мёртвое монтирование. **Признак, отличающий зависание от
  медленного чтения:** `/proc/diskstats` по `sdb` не меняется **вообще** — ноль операций за 10 с. Если
  счётчики растут, диск просто медленный и надо ждать, а не рвать. Лечение — только умерщвление самого
  демона `ntfs-3g` (от root); обход архива делать **строго по одному каталогу верхнего уровня за раз**.
- 🔴 **После смерти `ntfs-3g` том остаётся «эксклюзивно открыт», пока контейнеры держат его монтирование
  в своих namespace — и `fuser` при этом пуст.** 2026-09-20 повторный `mount` проходил молча и с `rc=0`,
  но обращение к каталогу сразу давало `Transport endpoint is not connected`, а запуск `ntfs-3g` вручную —
  `Mount is denied because the NTFS volume is already exclusively opened`. Держателя не видно ни в `ps`,
  ни в `fuser /dev/sdb1`: ссылку хранит **суперблок fuseblk**, живой до тех пор, пока на монтирование
  ссылается хоть один mount namespace. Считаются они так: `grep -l hdd2tb /proc/[0-9]*/mounts | wc -l`
  (в инциденте — 279 процессов при пустом `mount` на хосте). **Порядок восстановления:** убить `ntfs-3g` →
  `umount -l` → **остановить контейнеры-держатели** (`nasa_api`, `downloads`, `samba`, `nextcloud`) →
  `mount /mnt/hdd2tb` → поднять их обратно **только** через `up -d --force-recreate`. Проверять не статусом
  контейнера, а изнутри: `docker exec <c> ls /mnt/hdd2tb`.
- После временных замеров (`python3 -m http.server`) **гасить процесс** — иначе остаётся открытый порт на всю LAN (найден и закрыт при аудите 2026-08-10).
- **`172.29.172.1` — не универсальный адрес VPS, а «тот, кто окажется по этому адресу в текущей сети».** С рабочей станции это наш VPS (`OpenSSH_9.6p1`), с Vostro — **посторонняя машина** (`OpenSSH_8.9p1`): там поднят `awg0`, но маршрутов через него нет, и запрос уходит в чужой шлюз. Проверять принадлежность **баннером**, а не пингом; с Vostro использовать только публичный `95.163.176.103`.
- **`ffmpeg` съедает stdin** и вместе с ним остаток скрипта, переданного через `ssh "bash -s"` — дальше сыплется `команда не найдена` на ровном месте. Ставить `-nostdin` или `</dev/null`. Родня `sudo -S` + heredoc.
- 🔴 **`DOCKER-USER` на VPS пуст — контейнер с `-p 0.0.0.0` обойдёт ufw целиком.** Проверено 2026-08-13: `iptables -S DOCKER-USER` содержит только `-N DOCKER-USER`, ноль правил. Docker вставляет свои правила в `FORWARD` **до** цепочек ufw, поэтому ufw для опубликованных портов контейнеров не работает вовсе. Сейчас не стреляет: наружу опубликован только 443 (Amnezia). Но любой будущий `-p 0.0.0.0:PORT` окажется в интернете молча, вопреки `ufw status`. Находка соседнего проекта, подтверждена самостоятельно.
- **Порты VPS — общий ресурс с другим проектом владельца.** `10022` уже держит туннель Jetson. Прежде чем занимать порт на VPS — сверяться с `ss -tlnp` и с `HOST_CONTRACT.md` на Vostro.
- 🔴 **`id_token` — это тоже персональные данные, и фильтры его пропускают.** 2026-09-20 при разборе ответа
  авторизации Cloud.ru в вывод команды один раз попал полный `id_token`: редактор секретов ловил `token`
  и `access_token`, а отдельное поле `id_token` — нет. Внутри JWT в base64 лежат стандартные claim'ы OpenID,
  включая **почту и телефон владельца** — то есть утечка не токена, а персональных данных, и она переживает
  истечение самого токена (час). Правило: при работе с OpenID/OAuth фильтровать **все** поля, оканчивающиеся
  на `_token`, и не печатать тело ответа авторизации целиком — только длину и `expires_in`.
- 🔴 **Cloud.ru отвечает `503` вместо `404` и `400` — это «я не понял запрос», а не «сервис лежит».** Проверено 2026-09-20 перебором: у `https://containers.api.cloud.ru` обязателен query-параметр **`projectId`**; без него рабочий путь `/v1/containers` даёт `503`, с ним — `200`. С `projectId` двенадцать несуществующих путей тоже дают `503`, и только `containers` отвечает. Без токена те же пути дают `403 RBAC` — авторизация проходит раньше маршрутизации. Повтор с задержками 0/5/15/30/60/90 с ничего не меняет. ⚠️ Отдельно: имени `containerapps.api.cloud.ru` **не существует вовсе** (`curl` rc=6), API потребления живёт на `organization.api.cloud.ru`, а не на `billing.api.cloud.ru`. Полный разбор и отзыв неверного диагноза — `docs/integrations/sber/CLOUD_RU.md`, раздел «Проверенный доступ».
- 🔴 **Переменная, заданная только в `docker-compose`, не существует для хостовых скриптов.** Поймано 2026-09-20 при выкате D2: `TELEGRAM_PROXY` был прописан значением по умолчанию в compose (`${TELEGRAM_PROXY:-socks5://172.17.0.1:1080}`), и контейнер бота исправно ходил через SOCKS. Хостовой скрипт алерта читает `config/.env`, где переменной **не было вовсе**, поэтому пошёл в Telegram напрямую и упёрся в таймаут (`curl` код 28) — при том, что туннель был активен и порт слушал. Симптом выглядит как «Telegram недоступен», а на деле недоступна настройка. Правило: значение, нужное и контейнеру, и хосту, живёт в `config/.env`, а compose его только подхватывает. Проверять — прогоном самого скрипта (`--test`), а не установкой юнита: юнит встанет и с неверной настройкой.
- 🔴 **`.claude/settings*.json` накапливают НАСТОЯЩИЕ секреты.** В allowlist разрешённых команд попадают целые строки из истории вместе со значениями. Найдено инвентаризацией 2026-08-12: в копии проекта `NASA` лежит токен GitHub (`settings.local.json:167`, плюс копия в worktree), в соседнем проекте — sudo-пароль от Vostro открытым текстом. Файлы под `.gitignore`, наружу не уезжают, но при передаче машины, бэкапе профиля или отправке настроек кому-то — помнить.
- 🔴 **Отрицательный результат не называет причину — он называет только себя.** 2026-08-13 `ping` и `ssh` на Vostro молчали, и я построил на этом теорию про исключение `192.168.0.0/16` в split tunneling Amnezia, успев порекомендовать соседнему проекту менять настройку. Настоящая причина: адрес Vostro сменился при переустановке 11.08 (`192.168.75.177` → **`.153`**), и поправка **лежала в моём же локальном документе** — я не открыл его прежде, чем объяснять тишину. По правильному адресу: 0 % потерь, 0–1 мс, `ssh` пускает. Исключение `/16` ни при чём и, судя по маршруту, наоборот **сохраняет видимость корпоративной сети при поднятом VPN** (`192.168.75.0/24` идёт on-link через Ethernet). Признак ошибки: объяснение появилось раньше, чем была проверена входная величина.
- 🟠 **Общий `HOST_CONTRACT.md` нельзя восстанавливать из своей односторонней копии.** 2026-08-12 накат моей копии ужал файл с 673 строк до 180 и стёр весь раздел соседнего проекта; хуже потери то, что в моей версии висел устаревший открытый вопрос, который сосед принял бы за текущее состояние. Восстановил сосед, взяв мою версию целиком и дописав свою — итог 728 строк. Правило: **источник истины — только файл на ноутбуке**, свой раздел дописывать, а не перезаписывать файл; локальный `docs/local/VOSTRO_HOST_CONTRACT.md` — черновик. Страховка (принята у соседей): перед перезаписью сравнивать число разделов и останавливаться при уменьшении.
- **`config/certs/russian_trusted_bundle.pem` — сертификат, а не утечка.** Он закоммичен намеренно (нужен для TLS до GigaChat). Автоматические сканеры секретов регулярно принимают его за приватный ключ, потому что смотрят на `-----BEGIN` не разбирая тип. Секрет — только `BEGIN ... PRIVATE KEY`; здесь `BEGIN CERTIFICATE`, и он публичен по назначению.
- 🔴 **Смена IP ломает не один файл, а список файлов.** После 09.08 старый адрес VPS остался в `overwritehost`, `overwrite.cli.url`, `trusted_proxies` Nextcloud и в `/etc/nasa-monitor/nasa-monitor.env` — из-за первого **Nextcloud 13 дней отправлял каждого клиента, включая телефон в двух метрах от NAS, на `https://193.8.215.130:8443`**. Правильный вывод не «поправить адрес», а **перестать навязывать адрес**: снять `overwritehost`/`overwriteprotocol`, и каждый клиент останется там, откуда пришёл. Схема переживает следующую смену IP без правок.
- **Публичный IP VPS недостижим из-под собственного VPN** — он исключён из туннеля по определению, иначе VPN зациклится. Рабочий внешний адрес для клиента под VPN — **`172.29.172.1`**, а не `95.163.176.103`. Проверено замером с рабочей станции: публичные адреса — таймаут, внутренний — HTTP 200.
- **`$host` в nginx отбрасывает порт, `$http_host` — сохраняет.** На нестандартном порту (8080/8443) это молча ломает любой сгенерированный редирект: клиент уезжает на `:80`.
- 🔴 **Снятие `overwritehost` включает обратно проверку доверенного домена — и ломает внутренних клиентов, которые ходят по docker-имени.** Talk-бот опрашивает Nextcloud как `host.docker.internal`; этого имени не было в `trusted_domains`, и после правки каждый его long-poll стал возвращать `400` мгновенно вместо блокировки на 30 с → плотный цикл повторов, 213 % CPU у Nextcloud, `load average` 5.9. Лечится добавлением имени в `trusted_domains` (вернулся штатный `304`). Nextcloud отвечает на недоверенный домен кодом **`400` и HTML-страницей на 22 КБ**; клиент, ждущий JSON, видит «Bad Request», а настоящая причина — в теле: `{"error": "Trusted domain error.", "code": 15}`. **Меняя `overwritehost`, сразу перечисляй все имена, которыми к Nextcloud обращаются изнутри.**
- 🔴 **Симптом, замеренный внутри собственного окна правок, нельзя объяснять историей.** 2026-08-22 я увидел 400-е и нагрузку 5.9, объявил это застарелым дефектом «бот 12 дней бил в стену» и записал в документацию и в два коммита. Базовая линия говорила обратное: **вчера 13 626 опросов, все `304`, ноль `400`**; все 7 975 отказов уложились в один час — тот, в котором я снял `overwritehost`. То есть я нашёл, диагностировал и починил собственную регрессию, приписав её прошлому. Проверка стоила одной команды и не была сделана. Родня уже записанным граблям про Vostro: **объяснение появилось раньше, чем была проверена входная величина**. Правило: увидел аномалию — сначала посмотри тот же показатель ДО своей правки.
- **Локальная модель: две ловушки, обе пойманы замером 2026-08-22.** (1) Рассуждающие модели (`qwen3.5:4b`) кладут вывод в поле `thinking`, а `response` возвращают **пустым**, если бюджет токенов израсходован на размышление — ставить `"think": false`. (2) Тело запроса из PowerShell уходит **не в UTF-8**: модель получает `??????? ????????` вместо кириллицы и отвечает не на тот вопрос. Кодировать явно: `[System.Text.Encoding]::UTF8.GetBytes($json)`. После обеих правок — «Париж» за 0.9 с, 16 ток/с. Родня уже записанным граблям про `cp1251` в консоли.
- **Jetson дотягивается до модели на станции обратным туннелем — проверено, 0.637 с.** `ssh -R 11435:127.0.0.1:11434 admin@192.168.0.50` со **станции**: инициирует тот, кто может (Jetson за двойным NAT сам не может). Та же схема годится для `IMMICH_MACHINE_LEARNING_URL`.
- 🔴 **Таймаут вызывающей стороны короче таймаута той, кого она вызывает — вызывающая сторона сдаётся первой.** Поймано 2026-08-24: `@бобик` молчал (точнее, слал «🐕 Не смог получить ответ — попробуйте позже» вместо ответа) — Talk-бот ждёт LLM `talk_bot_llm_timeout=60` с, а сам LLM Gateway терпит локальную модель до `LLM_LOCAL_TIMEOUT=120` с. Если станция в этот момент отвечает медленнее 60 с (разово, например из-за параллельной нагрузки на тот же VPS-туннель), бот обрывает раньше, чем шлюз успевает — причём и раньше, чем шлюз успел бы упасть на DeepSeek. Симптом выглядит как «бот не ответил», а по факту ответ ушёл — просто извинение. Исправлено конфигом: `TALK_BOT_LLM_TIMEOUT="150"` в `.env` (> 120 с шлюза, без правки кода), `docker restart homecloud_nasa_api`. Правило: у каждого звена цепочки вызовов таймаут должен быть **строго больше**, чем у всех звеньев, которые оно вызывает.
- 🔴 **`.ps1` с кириллицей обязан быть сохранён в UTF-8 С BOM.** Без BOM Windows PowerShell 5.1 читает файл как cp1251, кириллица в комментариях разваливается, и одна искажённая последовательность рвёт строковый литерал — **скрипт перестаёт парситься целиком**. Симптом нулевой: запущенный скрыто, он просто ничего не делает, и увидеть ошибку можно только запустив его в открытую. Поймано 2026-08-23 на скрипте туннеля; проверка добавлена в ворота (`check_ps1_bom.py`).
- 🔴 **`ssh -R` не виден из контейнеров, пока `GatewayPorts` = `no`.** По умолчанию обратный проброс привязывается только к `127.0.0.1` хоста, а контейнер живёт в другом сетевом namespace и видит хост как `host.docker.internal` → **`172.17.0.1`** (docker0). Лечится `GatewayPorts clientspecified` в `sshd_config` **плюс** явной привязкой: `ssh -R 172.17.0.1:11435:...`. Наружу в LAN при этом ничего не открывается. Правя sshd на единственном пути доступа — обязательно `sshd -t` **до** применения и `reload`, а не `restart`.
- **Процессы, запущенные из сессии агента, её не переживают.** `Start-Process ... -WindowStyle Hidden` умирает вместе с командой. Для долгоживущего фонового процесса — `Invoke-CimMethod Win32_Process Create` (отсоединённый) либо автозапуск пользователя (`shell:startup`); регистрация задачи через `Register-ScheduledTask` требует прав администратора и без них падает с `Access is denied`.
- 🔴 **Выключенный клиент `ssh -R` оставляет слушатель на сервере ещё ~2 часа.** При `ClientAliveInterval 0` (умолчание) sshd не опрашивает клиента, и мёртвую сессию убирает только TCP keepalive: 7200 с + 9×75 с. Станция включается обратно, туннель не может занять порт и с `ExitOnForwardFailure=yes` выходит — цикл переподключения два часа получает отказ. **Симптома нет:** шлюз просто не видит локальную модель и уходит к внешнему провайдеру, дом работает, вопросы тихо уезжают наружу. Лечится `ClientAliveInterval 60` + `ClientAliveCountMax 3` (отвал за ~3 мин). Поймано 2026-08-23 **до** того, как выстрелило — вопросом «что сломается, когда владелец выключит компьютер», а не проверкой текущего состояния.
- 🔴 **Журнал в tmpfs стирает ровно то событие, ради которого он нужен.** `Storage=auto` без каталога `/var/log/journal` = журнал только в памяти, и после перезагрузки `journalctl --list-boots` знает одну загрузку. Причину аппаратного сброса 17.08 удалось восстановить лишь потому, что она случайно уцелела в трёх других местах: `/sys/fs/pstore/console-ramoops-0` (буфер в ОЗУ, переживает сброс), `wtmp` и ротированный `syslog.N.gz`. Это везение, а не диагностика. На Jetson включён `Storage=persistent` с жёстким пределом 200 МБ — корень живёт на SD-карте.
- **Аппаратный сброс отличается от программного тремя признаками, и все три надо смотреть вместе:** отсутствие парной строки `shutdown` в `last -x`; пустой или безобидный `/sys/fs/pstore/console-ramoops-0` (паника оставила бы трассу); `dmesg | grep "PMC reset source"` — у Tegra это `TEGRA_POWER_ON_RESET` против программной перезагрузки. ⚠️ Провал питания и срабатывание PMIC-watchdog дают **одинаковую** картину: различить их этими данными нельзя, и записывать надо неизвестное, а не удобную версию.
- **AmneziaVPN Desktop (Windows) не умеет генерировать нового клиента через свою SSH-логику — обход через `wg`/`awg` напрямую.** 2026-08-25: приложение показывало `ErrorCode 305` (таймаут), затем «SSH запрос отклонён». Проверено и отклонено: смена IP VPS (TCP до старого и нового IP — одинаково ~50 мс), диск/память/docker на VPS, лимит по частоте на 22 порту, исчерпание пула IP. `sshd` на `LogLevel DEBUG3` (включено и снято безопасно через `reload`, не `restart`) показал: транспорт и авторизация здоровы, но сессии приложения либо не доходят до `exec`, либо рвут канал сразу после — `clientsTable`/`awg0.conf` ни разу не изменились. Причина осталась в самом приложении (лога на диске нет, список серверов в реестре зашифрован) — дальше копать не стали. **Рабочий обход:** `wg genkey`/`pubkey`/`genpsk` внутри контейнера `amnezia-awg2` → `wg set awg0 peer ... allowed-ips 10.8.1.X/32` (живьём, без перезапуска) → тот же блок дописать (`>>`) в `/opt/amnezia/awg/awg0.conf` с бэкапом `.bak.<unixtime>` → собрать клиентский `.conf` вручную, скопировав параметры обфускации `Jc/Jmin/Jmax/S1-S4/H1-H4` из `[Interface]` сервера (без них хендшейк не пройдёт). Подтверждено рабочим клиентом. Разбор: `docs/plans/CHECKPOINT_2026-08-25.md`.
- 🔴 **Шкала «сигнал хороший» в приложении Deco не означает работающий транзит.** 2026-09-12: приложение показывало `Good` для дальнего узла, а замер давал **6 ответов из 10 со средним 746 мс и худшим 4023 мс**. Шкала меряет уровень сигнала, а не потери и не полосу. После переноса узла ближе — 10 из 10 и 53 мс, станция с 36 % / 175 Мбит/с ушла на **85 % / 866.7 Мбит/с**. У Deco E4 **нет выделенного радио под backhaul**: клиенты и транзит делят эфир, поэтому размещение узла перевешивает любую настройку. Разбор — `docs/34_NETWORK_SNAPSHOT_2026-09-12.md`.
- 🔴 **Два сервера Samba делили порт 445, и шара `hdd2tb` была недоступна.** Найдено 2026-09-12: кроме контейнера `homecloud_samba` (шары `public`/`nextcloud`/`immich`/**`hdd2tb`**, пользователь `nas`) на хосте жил **рудиментный smbd вне проекта** (шары `public`/`exchange`/`family`, **все пусты**, пользователь `admin`). Контейнер слушает `0.0.0.0:445`, хостовой — `192.168.0.50:445`; **конкретная привязка выигрывает**, и клиент из LAN попадал на пустой сервер, получая «Отказано в доступе» с верным паролем. Это **гонка при загрузке**: 08.09 хостовой стартовал на 24 с раньше контейнера. Лечение: `sudo systemctl disable --now smbd nmbd` (откат — `enable --now`). ⚠️ После починки `net use` всё равно падал с `системная ошибка 67`; сработал `New-SmbMapping`. Разбор — `docs/34_NETWORK_SNAPSHOT_2026-09-12.md` §5г.
- 🔴 **Под AmneziaVPN приложение Deco на телефоне не видит собственную домашнюю сеть.** 2026-09-12: приложение писало `Невозможно подключиться к Deco`, и это выглядело как пропавший из mesh узел. На деле оба узла были на схеме, связь зелёная, а «пропавший» узел в тот же момент нёс клиентов на **866.7 Мбит/с**. Причина: на Android в туннель уходит **весь** трафик, включая локальные адреса; на рабочей станции этого не видно — там в туннеле есть исключение для домашних подсетей. Опознаётся по значку VPN в статус-баре. **Прежде чем чинить сеть — выключить VPN на телефоне.**
- **Адрес узла Deco — не константа, а веб-интерфейс после обновления уходит на HTTPS.** Второй узел при перезагрузке переехал `.250 → .249`, станция `.103 → .102`; проверка по прежнему адресу дала «все 10 отказов» и выглядела как поломка. Главный узел после прошивки стал отвечать на `http://` кодом **`307` на `https://…:443`** — проверка, ждущая `200` по HTTP, объявляет живой узел мёртвым. Родня уже записанным граблям про Vostro: отрицательный результат не называет причину.
- **Прошивки Deco с пометкой `RSA2048_up` не ставятся поверх старой схемы подписи.** С 1.0.0 (2023) прямая заливка 1.3.1 отвергается (`Upgrade failed. Please select the latest version`). Цепочка: `1.0.0 → 1.2.0 → 1.2.2 → 1.3.1`. Отказ безопасен, запись не начинается. И каталоги TP-Link различаются: `deco-e4` ≠ `deco-e4r`, страница `v4` ≠ `v4-40` — вывод «файлов нет нигде» был сделан по неверному каталогу и оказался неверным.
- **`Invoke-WebRequest` занижает скорость скачивания в разы, а `getent hosts` отдаёт AAAA первой.** Первый замер Wi-Fi дал 10 Мбит/с, `curl` на том же канале в ту же секунду — 53–80. Проверка доступности TP-Link по адресам из `getent hosts` ушла в таймаут по IPv6, которого в доме нет вовсе, и выглядела как блокировка; по IPv4 всё было открыто. Полосу мерить `curl`, адреса брать `getent ahostsv4`.
- **Отчёт, каждый день показывающий ❌ на штатное состояние, — сломанный отчёт.** Три креста «EXTERNAL ACCESS» были нормой с Фазы 1 (порты закрыты для всех, кроме VPN) и приучили их не читать. Мерить надо цепочку, от которой зависит человек (на самом VPS против `127.0.0.1`), а тревожиться — на **открытый** наружу порт, а не на закрытый.

**🔜 Ближайшие задачи (канон — сводная редакция `docs/plans/DEVELOPMENT_PLAN_2026-09_SBER_ERA.md`, очередь A–F от 2026-09-19; список ниже частично устарел, приоритет у плана):**
- ✅ **Этап A выкачен 2026-09-19** (recovery SSD, шлюз, токен). Откат — `~/stageA-rollback/` + таблица в `DEPLOY_STAGE_A_2026-09.md`.
- ✅ **Этапы B и C выкачены 2026-09-19.** S3 (B3) отложен владельцем. E2 и D3 — в git, ждут «деплой».
- 🟠 **Telegram-бот + качалка (E9), приоритет владельца** (запрос сына) — обе спецификации утверждены
  (`docs/superpowers/specs/2026-09-19-telegram-family-bot-design.md`,
  `docs/superpowers/specs/2026-09-19-home-downloader-design.md`, ред. 1–5); **✅ выкачено на Jetson 2026-09-19 17:03 UTC** (`DEPLOY_DOWNLOADER_2026-09.md`): контейнер `homecloud_downloads` (aria2+AriaNg :6880), SOCKS `nas_jetson_nano-tg-socks` на 172.17.0.1:1080, бот в группе «Боровские» (приватность выкл.), объявление семье отправлено. ⚠️ dockerd не резолвит имена (ходит на 127.0.1.1 после смены resolv.conf 18.09) — новые образы не скачать до перезапуска Docker; таймеры скорости по UTC (день = 11:00–02:00 МСК).
- 🟠 **Решения владельца D1–D6** (план §2.2): ИБП, что класть в S3, копия архива 1.4 ТБ, Immich ML, smart routing, окно Part B.
- 🔴 **P0 deploy W1+W2** when Jetson on + «деплой»: `DEPLOY_FULL_SBER_CUTOVER.md` (Giga-2 cutover + Immich→HDD). Pack is in git.
- 🟠 **Cloud.ru balance/grant** — FM key exists but chat **402**; then optional `CLOUDRU_FM_API_KEY` on device.
- 🟠 **W3 S3** restic L2 off-site (after bucket). Vostro **out** of NAS architecture (ADR-0007).
- ✅ GitVerse `NAS_HOME` mirror (HTTPS); keep main=master aligned on push.
- 🟠 **P1 — EOL Ubuntu 18.04** — accept risk / ESM / board replacement.
- **P2 — coturn** or document Talk video LAN-only.
- 🟠 **Immich ML на ROG (пилот)** — начат 2026-09-12, **заблокирован**: на станции выключен гипервизор Windows (`HypervisorPresent=False`), Docker Desktop не стартует. Нужны админ-права + **перезагрузка** (`dism … VirtualMachinePlatform`, `bcdedit /set hypervisorlaunchtype auto`). Владелец отложил на 13.09. Подготовка сделана: образ под **2.7.5**, env вне git, порт на `127.0.0.1`. ⚠️ Связь — **обратным туннелем на `172.17.0.1`**, не по LAN: станция за Deco, Jetson её не достаёт.
- 🟠 **Deco: `living_room` на прошивке 1.0.0** — довести до 1.2.0 переподключением узла в приложении. До этого **не менять имя сети и пароль Wi-Fi**.
- Optional: скан эфира, Wi-Fi на EC220, Part B rename, SSD pull-test.
- Board: answer `work` Sber asks (`AGENTS.md` §8).

## Железо и доступ

| Компонент | Адрес / Путь | Примечание |
|---|---|---|
| Jetson Nano | `192.168.0.50` | LAN, статический IP |
| SSH на Jetson (из дома) | `ssh admin@192.168.0.50` | key-based; работает **только из домашней сети** `192.168.0.0/24` |
| SSH через VPS (извне) | `ssh -i ~/.ssh/borovskoy_new_ed25519 root@95.163.176.103` → `ssh -p 10022 admin@127.0.0.1` | рабочий путь из внешней сети |
| SSH одной командой (ProxyJump) | `ssh -o "ProxyCommand=ssh -i ~/.ssh/borovskoy_new_ed25519 -W %h:%p root@95.163.176.103" -p 10022 admin@127.0.0.1` | позволяет `bash -s < script.sh` напрямую |
| sudo на Jetson | `sudo -S <cmd>` | пароль = `NEXTCLOUD_ADMIN_PASSWORD` из `~/nasa/config/.env` **на устройстве**; не коммитить |
| VPS (AEZA, Frankfurt DE) | `95.163.176.103` | старый `193.8.215.130` заблокирован из РФ; IPv6 `2a12:5940:665a::2/48` |
| VPS через VPN-туннель | `172.29.172.1` | запасной путь, если публичный IP снова заблокируют |
| Сервисы VPS | только через VPN | ufw пускает сервисные порты лишь с `172.29.172.0/24` и `10.8.1.0/24` |
| Keenetic Omni KN-1410 | — | ⛔ План закрыт; холодный резерв |
| TP-Link Deco E4 ×2 | `192.168.0.1` / `.3` (план) | Заменяет роутер, **режим Router**; см. `docs/27_HOME_NETWORK_MESH.md` |
| Vostro 15 (личная машина владельца) | **`192.168.75.153`** ⚠️ (было `.177` до переустановки 11.08) | `ssh alexey@192.168.75.153`, по ключу, без пароля. ✅ Проверено 2026-08-13: up 1 д 1 ч, `enp3s0 192.168.75.153/24`, `amn0 10.8.1.26/32`. **Доступ есть только пока рабочая станция в корпоративной сети** — её Ethernet `192.168.75.137/24`, и ufw соседей пускает 22/tcp именно с `192.168.75.0/24`. Из дома машина недоступна, и это правило, а не поломка. Dell Vostro 15 (2018), **Linux Mint 22.3 XFCE**, user `alexey`. 2 ядра, 3.7 ГБ, **825 ГБ свободно**, **CUDA нет**. Роль: **off-site бэкап + внешний сторож**, не ML. Машину делит второй проект владельца → реестр занятого в `HOST_CONTRACT.md` **на самом ноутбуке** (728 строк, 7 разделов). См. `docs/plans/VOSTRO_ML_NODE_ONBOARDING.md` |
| Mercurial-клон на Vostro | `/home/alexey/hg/qspectr2` | `default = ssh://hg@rserver3/projects/Spectr2/qspectr2`. Ключ `~/.ssh/id_rsa_hg`, работает без проброса агента. Проверено 2026-08-13: рабочая копия чиста |
| Репо на Jetson | `~/nasa` | `/home/admin/nasa` (remote `Nasa_home.git`; rename не выкатан) |

## GitHub CLI (gh)

**Установлен:** `C:\tools\gh\bin\gh.exe` (также в PATH → работает как `gh` из Git Bash и PowerShell)  
**Авторизован:** keyring (Windows), токен с полными правами `repo` + `admin`

```bash
# Из Git Bash (рекомендуется):
gh repo view                          # инфо о репозитории
gh issue list                        # список issues
gh issue create --title "..." --body "..."
gh pr list                           # список PR
gh pr create --title "..." --body "..."
gh release list                      # список релизов
gh release create v1.x.x --notes "..."
gh api repos/AlexeyBorovskoy/NAS_Jetson_Nano/topics  # topics

# Из PowerShell (полный путь или просто gh если PATH обновлён):
& "C:\tools\gh\bin\gh.exe" issue list
```

**Если `gh` разлогинился:**
```bash
# Получить токен из git credential manager:
printf 'protocol=https\nhost=github.com\n' | git credential fill | grep password
# Авторизовать:
echo "ghp_TOKEN" | gh auth login --with-token
```

## Частые операции

### Коммит и пуш (из Windows, рабочая директория репо)
```bash
cd "e:/Linux mint/virtual_VM/shared/NAS_Jetson_Nano"
git add <files>
git commit -m "тип: описание"
git push
```

### SSH-команда на Jetson
```bash
# из домашней сети:
ssh -o ConnectTimeout=10 admin@192.168.0.50 "команда"

# из внешней сети (через VPS):
ssh -i ~/.ssh/borovskoy_new_ed25519 root@95.163.176.103 \
    "ssh -p 10022 admin@127.0.0.1 'команда'"
```

### Прогнать скрипт на Jetson извне (ProxyJump)
```bash
ssh -o "ProxyCommand=ssh -i ~/.ssh/borovskoy_new_ed25519 -W %h:%p root@95.163.176.103" \
    -p 10022 admin@127.0.0.1 "bash -s" < local_script.sh
```

### Git pull на Jetson
```bash
ssh admin@192.168.0.50 "cd ~/nasa && git pull --ff-only"
```
> ⚠️ На устройстве есть незакоммиченные ручные правки (см. «Расхождение git ↔ устройство») — `git pull` их затрёт.

### Docker Compose на Jetson
```bash
ssh admin@192.168.0.50 "cd ~/nasa && docker compose -f docker/compose/docker-compose.monitoring.yml --env-file config/.env up -d"
```

### Проверить, что бэкапы живы (не верить статусу юнита!)
```bash
ssh admin@192.168.0.50 "ls -lt /mnt/storage/backups/database-dumps/ | head -5"
```

### Создать GitHub issue
```bash
gh issue create \
  --title "Заголовок" \
  --body "Описание" \
  --label "enhancement"
```

### Создать release
```bash
git tag -a v1.x.x -m "описание"
git push origin v1.x.x
gh release create v1.x.x --title "v1.x.x — название" --notes "описание"
```

## Структура проекта

```
docker/compose/   — Docker Compose файлы для всех сервисов
config/           — конфиги (шаблоны); .env — НЕ в git
scripts/          — bash/python скрипты (backup, monitoring, setup, storage)
systemd/          — systemd units (таймеры, сервисы)
docs/             — документация (00–26 + подпапки)
docs/prompts/     — агентные промпты (CODEX_*, Claude, ChatGPT)
docs/quality/     — test plan, matrix, baseline reports
docs/references/  — внешние ссылки (external_docs/ — gitignored)
docs/local/       — локальные runbook'и вне git (.git/info/exclude)
tests/            — автоматические тесты (network, service, storage, backup)
assets/           — фото, схемы, скриншоты
artifacts/        — отчёты аудитов, JSON exports
archive/legacy/   — устаревшие файлы (не удалять, хранить)
.github/          — CI/CD workflows, issue templates
```

> **Полная карта структуры:** `docs/REPOSITORY_STRUCTURE.md`

## Сервисы и порты (Jetson 192.168.0.50)

| Сервис | Порт | URL |
|---|---|---|
| Nextcloud | 8080 | http://192.168.0.50:8080 |
| Immich | 2283 | http://192.168.0.50:2283 |
| LLM Gateway | 8090 | http://192.168.0.50:8090/health |
| API (`homecloud_nasa_api`) + Swagger | 8099 | http://192.168.0.50:8099/docs |
| Netdata | 19999 | http://192.168.0.50:19999 |
| Uptime Kuma | 3001 | http://192.168.0.50:3001 |
| Portainer | 9000 / 9443 | http://192.168.0.50:9000 (9443 — HTTPS) |
| Samba | 445 | `\\192.168.0.50\public`, `\\192.168.0.50\hdd2tb` |
| Beszel Agent | 45876 | внутренний (→ Hub через туннель) |

**Какой адрес откуда** (пересмотрено 2026-08-22 — единого «внешнего адреса» нет):

| Откуда | Адрес | Примечание |
|---|---|---|
| Дома, Wi-Fi за Deco (`192.168.68.x`) | `http://192.168.0.50:<порт>` | через NAT Deco, работает |
| Дома, проводом в EC220 | `http://192.168.0.50:<порт>` | — |
| Снаружи под VPN | **`http://172.29.172.1:<порт>`** | единственный рабочий внешний путь |
| Снаружи без VPN | нет | и не должно быть — правило №4 |
| По имени | `borovskoy.dynv6.net` → `95.163.176.103` | сервисные порты всё равно только из VPN |

> 🔴 **Публичные IP VPS в клиентах не прописывать.** Из-под собственного VPN они
> недостижимы конструктивно (исключены из туннеля), без VPN — закрыты ufw.
> Именно на этом 13 дней не работала синхронизация телефонов.

> ⚠️ Внутри домашней LAN все сервисы открыты **любому, кто знает пароль Wi-Fi** — сегментации нет.

## Экономия токенов: какую модель на что тратить

> Принята 2026-08-13. Источник — `Cost-Aware Model Routing Policy`.
> Оптимизируется **не цена одного вызова, а полная стоимость решённой задачи**.

### Правило выбора

| Модель | Для чего |
|---|---|
| **Haiku** | механика: Glob/Grep, чтение логов и выгрузок, сбор фактов, прогон тестов, форматирование, массовые однотипные операции, подготовка контекста для другой модели |
| **Sonnet** | основная инженерная работа: реализация, рефакторинг, тесты, обычный debugging, code review, анализ архитектуры |
| **Opus** | только при объективной причине: решение затрагивает несколько подсистем, конкурирующие подходы, сложный root-cause, Sonnet уже пробовал и не смог, security review |
| **Fable** | крупные автономные исследования, выходящие за обычный цикл |

Эскалация `Haiku → Sonnet → Opus` **только по причине** (не хватило контекста,
не прошла проверка, задача оказалась сложнее оценки), а не по умолчанию.

### Основная сессия — supervisor

Декомпозировать → выбрать минимально достаточную модель на подзадачу →
делегировать → собрать → проверить противоречия → эскалировать **только
проблемную часть**. Не отдавать всю задачу Opus, если Opus нужен для её десятой доли.

### Что реально экономит здесь

Проверено на практике 2026-08-12:

- **Не тянуть сырьё в контекст.** Скрипт должен считать и вернуть ответ, а не дамп,
  который потом разбирается. Худший случай: читал 254 строки чужого `settings.json`,
  чтобы узнать наличие одного блока — хватило бы `grep`.
- **Объёмное чтение — в Haiku-subagent.** Обратно приходит сводка, а не весь вывод.
- **Батчить проверки** в один вызов вместо трёх.
- 🔴 **Дороже всего обходится повторная работа, а не дорогая модель.** `E:` вместо `E:\`
  стоило полного пересбора скана; кириллица в `.ps1` под cp1251 — трёх заходов;
  `ssh`, съевший stdin, — двух прогонов. Приоритет «минимизация повторной работы»
  стоит **выше** экономии.

### Порядок приоритетов при конфликте

```
1. корректность  2. безопасность  3. воспроизводимость
4. минимизация повторной работы   5. экономия   6. скорость
```

⚠️ **Экономия — пятая, не первая.** Самое ценное в аудите 2026-08-12 родилось из
сомнения в собственных числах (4 → 19 → 14 значений в git) и из вопроса «а точно ли
диск просканирован». Дешёвая модель выдала бы те же неверные числа, просто дешевле.
Механику — вниз, сомнение — оставлять здесь.

### Отчётность

Не докладывать о выборе модели на каждом шаге. При существенной эскалации — одна
строка с причиной. Решилось дёшево — молча продолжать.

## Субагенты

> Принято 2026-08-22 по требованию владельца.

### 🔴 Язык — русский. Всегда и у всех

**Все агенты проекта, включая субагентов, общаются по-русски.** Это относится к
отчётам, заголовкам, пояснениям и к тексту, который уходит владельцу.

⚠️ **Субагент этого правила не наследует.** Он стартует с холодным контекстом, и
если в промпте не сказано — ответит по-английски. Поэтому **первой строкой промпта
субагента писать явно:**

```
ВАЖНО: отвечай ТОЛЬКО НА РУССКОМ ЯЗЫКЕ. Весь отчёт, заголовки и пояснения — по-русски.
```

Проверено на практике: два субагента 2026-08-22 вернули отчёты по-английски именно
потому, что указания не было.

### Как ставить задачу субагенту

- **Требовать разделения фактов и суждения.** Отдельный раздел «оценка» в конце.
  Иначе догадка приезжает с той же интонацией, что и замер.
- **Явно разрешать отрицательный ответ.** «Если не переносится — скажи прямо,
  отрицательный ответ ценнее уклончивого». Работает: оценка по Kaggle вернулась
  честным «нет» с обоснованием, а не вежливым «может быть».
- **Требовать пути и номера строк** для каждого факта — иначе проверить нечем.
- **Запрещать печать секретов**: называть файл и строку, значение — никогда.
- **Чужие проекты — только чтение.** Для этого брать агента `Explore`: у него
  физически нет `Write`/`Edit`, и это надёжнее просьбы «ничего не меняй».
- Возвращать **сводку, а не дамп**: субагент существует, чтобы объёмное чтение не
  попало в основной контекст.

### Когда не звать субагента

Если задача решается двумя `grep` — делать самому. Субагент стартует вхолодную и
заново выводит контекст, который у основной сессии уже есть; на мелочи это дороже.

### Оркестрация: какая модель какому субагенту

> Принято 2026-08-22. Основная сессия работает **диспетчером**: разбирает задачу,
> назначает каждой части минимально достаточную модель, собирает результаты и
> проверяет их на противоречия. Не отдавать всё дорогой модели, если дорогая нужна
> для десятой доли.

| Модель | Какие задачи субагента | Признак, по которому узнать |
|---|---|---|
| **Haiku** | механика: обход дерева, `Glob`/`Grep`, чтение логов и выгрузок, инвентаризация, подсчёт, приведение к формату | ответ **не требует суждения**; правильность видна сразу; ошибка дешева |
| **Sonnet** | исследование с выводом: разбор чужого проекта, диагностика на живой системе, сравнение вариантов, проверка фактов в вебе | нужен вывод, но задача **в одной области** |
| **Opus** | несколько подсистем сразу, конкурирующие подходы, сложный root-cause, security-разбор, либо Sonnet уже пробовал и не смог | эскалация **по названной причине**, не по умолчанию |
| **Fable** | крупные автономные исследования вне обычного цикла | отдельное решение |

**Жёсткие поправки к таблице:**

- 🔴 **Живая система с необратимыми действиями — не Haiku.** Диагностика диска, на
  котором лежит единственная копия архива, стоит дороже разницы в цене модели.
- 🔴 **Проверка фактов о внешнем мире — не по памяти.** Цены, тарифы, условия
  сервисов субагент обязан смотреть в вебе; в промпте это писать явно.
- **Сомнение не делегируется.** Дешёвая модель выдаст те же неверные числа, просто
  дешевле. Вопрос «а точно ли это так» остаётся у основной сессии.

**Как экономить на самом деле:**

1. **Сводка вместо дампа.** Главная экономия не в цене модели, а в том, что объёмное
   чтение **не попадает в основной контекст**. Требовать краткий отчёт с путями.
2. **Параллелить независимое.** Два субагента на разные вопросы запускаются одним
   ходом и работают одновременно; цепочка из двух — вдвое дольше без выигрыша.
3. **Оценивать до запуска.** Решается за три вызова инструментов — делать самому.
4. **Один субагент — одна задача.** Два вопроса в одном промпте дают два
   поверхностных ответа вместо одного полного.
5. **Эскалация только по причине.** Вернулось «не хватило контекста» или ответ не
   прошёл проверку — перезапустить на модели выше, назвав причину одной строкой.
   Не спорить с дешёвой моделью дважды: второй заход дороже сразу правильного.

⚠️ **Приоритет прежний:** корректность → безопасность → воспроизводимость →
**минимизация повторной работы** → экономия → скорость. Экономия пятая. Повторный
прогон из-за сэкономленной модели съедает экономию с запасом.

## Жёсткие правила

1. **НЕ коммитить** реальные `.env`, пароли, токены, ключи, персональные данные.
2. **НЕ трогать** Amnezia VPN контейнеры на VPS — уронит ~25 VPN клиентов.
3. **НЕ удалять** сетевой профиль LAN на Jetson (на устройстве он называется `nasa-lan`, eth0, 192.168.0.50/24).
4. **НЕ открывать** сервисы напрямую в интернет без отдельного решения. Наружу на VPS допустимы только 22, 443 и 40568/udp.
5. **Destructive команды** (rm -rf, форматирование, DROP DATABASE) — только с явного подтверждения.
6. Перед push: `./scripts/security/check_no_secrets.sh`
7. **НЕ форматировать** `/mnt/hdd2tb` — на нём 1.4 ТБ семейного архива, NTFS оставлен намеренно.
8. Любое значение с пробелом в `config/.env` — **обязательно в кавычках** (см. «Грабли»).
9. Любое сетевое оборудование сначала подключать изолированно к workstation: не вводить в production LAN с активным DHCP и не делать factory reset без явного подтверждения.
10. Deco E4 вводится **в режиме Router** и обязан быть переведён на `192.168.0.1/24` (`More → Advanced → LAN IP`) **до** подключения Jetson. Подсеть `192.168.0.0/24` и адрес `.50` не менять никогда (правило №3).
11. **Не выводить EC220-G5 и не сбрасывать его** до успешной приёмки новой сети — это единственный путь отката.
12. Перед заменой роутера **записать тип WAN-подключения и WAN MAC** со старого роутера. Без них новую сеть не поднять, а старого роутера под рукой уже не будет.
13. **Любая работа сохраняет работоспособность VPN на VPS и не затрагивает конечных пользователей** (владелец, 2026-08-22). Проверять до и после: `amnezia-*` не перезапускались, наружу слушают только 22/443/40568, число пиров не уменьшилось. nginx менять через `nginx -s reload`, не пересозданием контейнера.
14. 🔴 **Ошибка ловится локально, а не в бою** (владелец, 2026-08-22). Полные требования — `docs/32_QUALITY_GATE.md`. Кратко и обязательно:
    - **Перед коммитом** `bash scripts/quality/preflight.sh` обязан пройти. Хук `.githooks/pre-commit` запускает его сам; включение на новой машине: `git config core.hooksPath .githooks` и `pip install vermin`. Обход `--no-verify` — только осознанно и с объяснением в сообщении коммита.
    - **Перед выкатом**: выкладывать код **вместе с тестами**; проверять синтаксис на самой цели её интерпретатором; иметь наготове `.bak` или доказанную команду отката.
    - 🔴 **После выката — убедиться, что работает новый код, а не файлы лежат на диске**: `systemctl show <unit> -p ActiveEnterTimestamp` против `stat` файла. Долгоживущий процесс импортирует модули один раз при старте; у соседнего проекта скопированные файлы плюс зелёные тесты стоили трёх часов ответов старым кодом.
    - 🔴 **Ворота не зависят от семантики shell.** `if ( set -e; ... )` — bash отключает `errexit` в условии. Код возврата брать явно: `out=$(проверка); rc=$?`. Первая версия проверки `.env` в этом проекте была написана неверно и пропустила файл с тем самым дефектом, что убил бэкапы на 16 дней.
    - **Каждая проверка обязана иметь свою историю дефекта.** Проверка без истории — балласт, который начнут обходить.
    - **Тишина ≠ успех.** Проверять поведение отдельной командой, а не отсутствием записей в журнале.
15. 🔴 **Документация и репозиторий на GitHub всегда отражают текущее состояние** (владелец, 2026-08-22). Изменил систему — в том же заходе поправь документацию и выложи. Условия:
    - **Вся документация двуязычная** (🇷🇺 / 🇬🇧). Новый документ без второго языка не считается готовым.
    - **Не раздувать.** Документ должен отвечать на вопрос, а не пересказывать проект заново. Устаревшее — переписывать, а не дописывать сверху. README — входная дверь, а не руководство: подробности живут в `docs/`.
    - **Числа приводятся с датой замера.** «13 контейнеров» без даты — это не факт, а воспоминание.
    - **Ошибку не удалять, а отзывать** — с тем, как она была найдена. Отозванный диагноз ценнее аккуратной истории.
    - Перед push — `./scripts/security/check_no_secrets.sh` (правило №6).

## Workflow (стандартная процедура)

1. Изменения в файлах проекта (Windows)
2. `git add` + `git commit`
3. `git push`
4. При необходимости применить на Jetson: `ssh admin@192.168.0.50 "cd ~/nasa && git pull --ff-only"` (из дома) или через VPS-jump (см. «Частые операции») — **сначала проверить, не затрёт ли ручные правки на устройстве**
5. Перед запуском Nextcloud/Immich/backup: `sudo bash scripts/storage/storage_preflight.sh`
6. Перезапуск затронутых контейнеров (если compose-файлы изменились и preflight прошёл)
7. После крупных изменений: `git tag` + `gh release create`
8. Обновить README и CHANGELOG

## Память

Память о проекте (cross-session): `C:\Users\Alexey\.claude\projects\e--Linux-mint-virtual-VM-shared-NAS-Jetson-Nano\memory\`
Индекс: `MEMORY.md` в той же папке.

## Связь со вторым проектом владельца

Владелец ведёт два проекта в двух сессиях Claude Code на одной машине; они делят
ноутбук `192.168.75.153` и VPS. Сессии друг друга не видят, поэтому заведён канал:

| Где | Для чего |
|---|---|
| `E:\agent_coordination\` (вне git) | **события и запросы** — «проверил, выяснилось вот что» и «поделись вот этим» |
| `HOST_CONTRACT.md` на ноутбуке | **состояние** — кто чем владеет прямо сейчас |

### Обмен по запросу (с 2026-08-13)

У сообщения есть **вид**. Доска перестала быть только односторонней рассылкой фактов:

| Вид | Что означает | Когда перестаёт всплывать |
|---|---|---|
| `fact` | «вот что я проверил». Ответа не требует | после `resolve` |
| `ask` | **запрос данных у соседа** | только когда лёг `answer` |
| `answer` | ответ на конкретный `ask` | сразу; **закрывает свой `ask` сам** |

```bash
python E:\agent_coordination\coord.py post --from nas --to work --subject "..." --body "..."
python E:\agent_coordination\coord.py post --from nas --to work --kind ask --subject "..." --body "..."
python E:\agent_coordination\coord.py post --from nas --to work --kind answer --re m0029 --subject "..."
python E:\agent_coordination\coord.py list --agent nas --open
python E:\agent_coordination\coord.py show m0029          # целиком, не разбирая inbox.jsonl
```

**Правила обмена по запросу:**

1. **`ask` — просьба, а не команда.** Что и в каком объёме отдавать — решает получатель.
   Отказ законен: `resolve` и строка почему. Автономия сессий не меняется.
2. **Спрашивать то, чего у себя нет.** Сосед — источник фактов о своей половине общих
   ресурсов, а не бесплатные руки. Что можно проверить самому — проверять самому.
3. **В запросе называть, что засчитается ответом.** Часто у соседа есть не то, что
   попросили, а эквивалент. И явно разрешать отрицательный ответ: «разницы не видно» —
   тоже данные.
4. **Отвечать своим состоянием, а не догадками о чужом.** Нет доступа проверить —
   так и писать: «принимаю как ваше свидетельство, независимо не проверено».
   Галочка без проверки хуже честного пробела.
5. **Чужое утверждение остаётся чужим, пока не проверено.** Своё — с доказательством
   (чем и когда замерено).
6. **Секретов в запросах и ответах нет** — как и в фактах. Папка вне git, но это
   не хранилище секретов.

Доставка автоматическая: хуки `SessionStart` / `UserPromptSubmit` в `.claude/settings.json`
обоих проектов (файлы под `.gitignore`). Открытый `ask` всплывает **на каждом ходу**,
пока на него не ответят; факт — один раз. Подробности — `README.md` в той же папке.

⚠️ **Порты VPS — общий ресурс.** Прежде чем занимать порт, сверяться с `ss -tlnp`
и с `HOST_CONTRACT.md`.
